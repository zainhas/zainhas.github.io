<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://zainhas.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://zainhas.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-08-02T06:51:48+00:00</updated><id>https://zainhas.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Advanced RAG Techniques</title><link href="https://zainhas.github.io/blog/2024/advanced-rag/" rel="alternate" type="text/html" title="Advanced RAG Techniques"/><published>2024-07-25T00:00:00+00:00</published><updated>2024-07-25T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2024/advanced-rag</id><content type="html" xml:base="https://zainhas.github.io/blog/2024/advanced-rag/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-07-25-advanced-rag/hero-480.webp 480w,/assets/img/posts/2024-07-25-advanced-rag/hero-800.webp 800w,/assets/img/posts/2024-07-25-advanced-rag/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2024-07-25-advanced-rag/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Advanced RAG Techniques" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>In this post we’ll dig into Retrieval-Augmented Generation (RAG) and show you practical tips and tricks to improve every part of the RAG pipeline! We’ll start by considering a a practical example: building a chatbot to emulate or assist a human doctor.</p> <p>Consider the typical North American doctor. After over a decade of education and treating around 100,000 patients, they have a pretty vast, intricate knowledge base. When diagnosing a patient, they retrieve relevant information from this extensive experience to provide accurate and context-specific advice.</p> <p>To create a chatbot using RAG that mimics this process, we need to replicate several key functions of a human doctor. First, we must establish a knowledge base equivalent to a doctor’s decade of study and patient interactions. This knowledge base should allow efficient searching, retrieving only the most pertinent information based on the patient’s current issue.</p> <p>Next, our system must be capable of reasoning over the retrieved information, akin to a doctor discerning the relevant details for a specific case. Explainability is also crucial, particularly in regulated fields where understanding the reasoning behind a diagnosis or treatment recommendation is as important as the recommendation itself.</p> <p>Moreover, the entire process must operate in real time. The system should handle patient queries, retrieve and reason over relevant knowledge, and provide actionable suggestions or diagnoses promptly.</p> <p>The simplest approach might involve feeding patient information into a language model, which then generates responses based on pre-trained medical data. Enhancements include fine-tuning the model on specialized medical literature and employing advanced prompt engineering techniques to improve performance.</p> <p>However, the more sophisticated RAG approach involves integrating a comprehensive knowledge base—such as medical histories and literature—into a retrieval system. By indexing and querying this extensive database, we can supply the language model with precise, relevant information to augment its generation capabilities</p> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image1.png" alt="RAG"/></p> <p>This is the simple RAG framework and for our purposes we will break this down into a three step process: indexing, retrieval and generation.</p> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image2.png" alt="RAGlabelled"/></p> <p>Throughout this post, we’ll explore each component of the RAG stack and discuss how to refine and enhance them, using open-source libraries and resources. Whether you’re developing a chatbot for healthcare or other applications, these techniques are generally applicable and will help improve most RAG setups!</p> <p>Let’s start!🚀</p> <h1 id="indexing">Indexing</h1> <p>Effective indexing is critical for any RAG system. The first step involves how we ingest, chunk, and store the data. Let’s explore some techniques to index data into a database, focusing on various methods for chunking text and leveraging metadata.</p> <h3 id="chunking-text">Chunking Text</h3> <h4 id="1-simple-chunking">1. Simple Chunking:</h4> <ul> <li> <p><strong>Overview</strong>: This method involves breaking documents into fixed-size chunks with some overlap. This ensures that each chunk retains context from the previous chunk.</p> </li> <li> <p><strong>Implementation</strong>: You can specify a chunk size (e.g., 35 characters) and an overlap size to maintain context across chunks. This approach works well for straightforward documents but might not be ideal for more complex texts and does not take advantage of the document structure at all.</p> </li> <li> <p><strong>Pros and Cons</strong>: Simple to implement but may break semantic units, leading to incoherent chunks and poorer retrieval performance.</p> </li> </ul> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image3.png" alt="textsplit"/> <a href="https://github.com/FullStackRetrieval-com/RetrievalTutorials/blob/main/tutorials/LevelsOfTextSplitting/5_Levels_Of_Text_Splitting.ipynb">Greg Kamradt (Data Indy)</a> - Text Splitting</p> <h4 id="2-semantic-chunking">2. Semantic Chunking:</h4> <ul> <li> <p><strong>Overview</strong>: Semantic chunking involves dividing text based on meaning rather than fixed sizes. This technique ensures that each chunk represents a coherent piece of information.</p> </li> <li> <p><strong>Implementation</strong>: Calculate the cosine distance between sentence embeddings. If two sentences are semantically similar (below a certain threshold), they belong in the same chunk. This creates variable-length chunks based on the content’s semantic structure.</p> </li> <li> <p><strong>Pros and Cons</strong>: Produces more coherent and contextually meaningful chunks, improving retrieval accuracy but requires forward passes through a BERT based encoder model which is more computationally intensive then simple chunking.</p> </li> </ul> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image4.png" alt="semchunk"/> <a href="https://github.com/FullStackRetrieval-com/RetrievalTutorials/blob/main/tutorials/LevelsOfTextSplitting/5_Levels_Of_Text_Splitting.ipynb">Greg Kamradt (Data Indy)</a> - Semantic Chunking</p> <h4 id="3-language-model-based-chunking">3. Language Model-Based Chunking:</h4> <ul> <li> <p><strong>Overview</strong>: This advanced method uses a language model to create self-contained propositions from text, ensuring that each chunk is semantically complete.</p> </li> <li> <p><strong>Implementation</strong>: A language model (e.g., a 7-billion parameter model) processes the text, breaking it into propositions that make sense independently. The model then combines these propositions into chunks, balancing semantic completeness and context. This method is computationally intensive but provides the highest accuracy.</p> </li> <li> <p><strong>Pros and Cons</strong>: Adapts to text nuances and results in high-quality chunks, but computationally intensive and may require fine-tuning for specific applications.</p> </li> </ul> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image5.png" alt="llmchunking"/> <a href="https://arxiv.org/abs/2312.06648">Dense X Retrieval: What Retrieval Granularity Should We Use?</a></p> <p>Model: https://huggingface.co/chentong00/propositionizer-wiki-flan-t5-large</p> <h3 id="leveraging-metadata">Leveraging Metadata</h3> <h4 id="1-adding-and-filtering-based-on-metadata">1. Adding and Filtering Based on Metadata:</h4> <ul> <li> <p><strong>Overview</strong>: Metadata provides additional context that can significantly improve retrieval accuracy. By incorporating metadata like dates, patient age, and preconditions, you can filter irrelevant information during searches. Filtering helps in narrowing down the search space, making the retrieval process more efficient and relevant.</p> </li> <li> <p><strong>Implementation</strong>: When indexing data, store metadata alongside the vectorized text. For instance, if indexing patient records, include metadata such as age, date of visit, and specific conditions. Use this metadata to filter search results, ensuring the system only retrieves relevant information.</p> </li> </ul> <p>For example, if a query relates to pediatric patients, filter out records of patients over 18 years old before performing the semantic search. This reduces the noise in retrieval results and enhances the relevance of the output.</p> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image6.png" alt="filters"/> <a href="https://medium.com/@myscale/optimizing-filtered-vector-search-in-myscale-77675aaa849c">MyScale</a></p> <h4 id="2-using-gliner-to-generate-metadata-for-chunks">2. Using GLiNER to Generate Metadata for Chunks</h4> <p>You might not always have extensive metadata to augment your chunks with but using the GLiNER (Generalized Linear Named Entity Recognizer) model you can generate metadata from chunks on the go! GLiNER is a robust BERT based model that can be used to tag and label chunks during the ingestion phase to create metadata on the go. This can significantly improving the retrieval process.</p> <ul> <li> <p><strong>Autolabeling with GLiNER</strong>:</p> <p><strong>Implementation</strong>: You can simply provide each chunk into GLiNER along with the tags you want to identify in the chunk. If the tags are found in the chunk text it will identify them if nothing matches with high confidence then tags wont be produced, reducing false positives!</p> <p><strong>Pros and Cons</strong>: Works quite well for general use but might require finetuning for niche and domain specific datasets and applications.</p> </li> <li> <p><strong>Query Parsing and Filtering</strong>:</p> <p><strong>Implementation</strong>: Another use for GLiNER is to parse incoming queries and match them against labeled metadata to filter results.</p> <p><strong>Pros and Cons</strong>: Enhances retrieval accuracy by ensuring contextual relevance but adds an extra processing step.</p> </li> </ul> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image7.png" alt="gliner"/> <a href="https://arxiv.org/abs/2311.08526">GLiNER: Generalist Model for Named Entity Recognition using Bidirectional Transformer</a> Demo: https://huggingface.co/spaces/urchade/gliner_multiv2.1</p> <p>These techniques lay the groundwork for a robust RAG system, enabling efficient retrieval from large datasets. The choice of chunking method and the use of metadata will depend on the specific requirements and characteristics of your dataset.</p> <h1 id="retrieval">Retrieval</h1> <p>Now let’s discuss the R part of the RAG setup! How can we enhance retrieval recall from a vector database? This process is all about retrieving as many of the documents relevant to a query, significantly improving the chances the LLM can then produce high quality results. Here are several techniques to achieve this:</p> <h3 id="1-hybrid-search">1. Hybrid search</h3> <p>Vector search which captures the semantic meaning of the query and keyword search which identifies exact matches for specific terms. Hybrid search combines the strengths of vector search and keyword search to enhance retrieval accuracy. In fields like medicine, many terms and concepts are not semantically understood but are instead specific keywords such as medication names, anatomical terms, disease names, and diagnoses.</p> <p>Pure vector search might miss these critical keywords, whereas keyword-based search ensures that specific, important terms are considered. By integrating both methods, hybrid search allows for a more comprehensive retrieval process.</p> <p>These search, vector and keyword, methods run in parallel, and the results are then merged and ranked according to a weighted system. For instance, using Weaviate, you can adjust the alpha parameter to balance the importance of vector versus keyword search results, creating a combined, ranked list of documents. This balances precision and recall, improving overall retrieval quality but requires careful tuning of weighting parameters.</p> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image8.png" alt="hybsearch"/></p> <h3 id="2-query-rewriting">2. Query rewriting</h3> <p>Query rewriting acknowledges that human queries may not be optimized for vector databases or language models. By leveraging a language model to rewrite queries, the retrieval process can be significantly improved.</p> <ul> <li><strong>Query Rewriting for Vector Databases</strong>: The patient’s initial query is transformed into a more database-friendly format. For example, “My left shoulder hurts and I have numbness in my thumb and index finger” might be rewritten as “Pain left shoulder. Numbness. Numbness index finger.”</li> <li><strong>Prompt Rewriting for Language Models</strong>: This involves the automatic and interactive automatic construction of prompts to optimize interaction with the language model, enhancing the quality and accuracy of the generated results. Frameworks like <a href="https://github.com/stanfordnlp/dspy">DSPy</a> help with this process.</li> </ul> <p>These rewritten queries and prompts ensure that the search process better understands and retrieves relevant documents and the language model is prompted optimally. Query rewriting can be achieved as demonstrated below.</p> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image9.png" alt="rewrite"/> <a href="https://arxiv.org/abs/2305.14283">Query Rewriting - Ma et al. 2023</a></p> <h3 id="3-fine-tuning-embedding-models">3. Fine-Tuning Embedding Models</h3> <p>While pre-trained embedding models provide a good starting point, fine-tuning these models on your specific dataset can dramatically improve retrieval performance. This involves:</p> <ul> <li> <p><strong>Selecting Appropriate Models</strong>: For specialized fields like medicine, choose models pre-trained on relevant data. For this particular examples I chose to use the MedCPT family of query and document encoders that have been pre-trained by an unprecedented scale of 255M query-article pairs from PubMed search logs.</p> </li> <li> <p><strong>Fine-Tuning with Positive and Negative Object Pairs</strong>: You can further improve the performance of these models by collecting your own data and creating pairs of similar (positive) and dissimilar (negative) examples, and fine-tune the model to better understand these distinctions. This helps the model learn domain-specific relationships and semantics, improving retrieval performance but requires carefully curated training data.</p> </li> </ul> <p>These techniques, when combined, create a robust retrieval system that enhances the overall relevance of objects that when eventually given to a LLM improve generation quality.</p> <h1 id="generation">Generation</h1> <p>Lastly, let’s discuss how you can improve control the generation quality of a Language Model (LLM). Here the name of the game is again to make sure the context you give to your LLM is as relevant to the prompt as possible, irrelevant data could trigger hallucinations. Here are some tips to achieve better generation results:</p> <h3 id="1-autocut-to-remove-irrelevant-information">1. Autocut to Remove Irrelevant Information</h3> <p>Autocut is a method to filter out irrelevant information retrieved from the database, which can otherwise mislead the LLM and cause hallucinations. Here’s how it works:</p> <ul> <li> <p><strong>Retrieve and Score Similarity</strong>: When a query is made, multiple objects are retrieved along with their similarity scores.</p> </li> <li> <p><strong>Identify and Cut Off</strong>: Using the similarity scores, identify a cutoff point where the scores drop significantly. Objects beyond this point are considered less relevant and are automatically excluded.</p> </li> </ul> <p>For example, if you retrieve six objects, the similarity scores might drop sharply after the fourth object. By examining the rate of change of the similarity scores, you can determine which objects to exclude, ensuring only the most relevant information is fed to the LLM.</p> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image10.png" alt="Autocut"/> <a href="https://docs.weaviate.io/weaviate/api/graphql/additional-operators#autocut">AutoCut</a></p> <h3 id="2-reranking-retrieved-objects">2. Reranking Retrieved Objects</h3> <p>Reranking involves using a secondary, more sophisticated model to reassess and reorder the initially retrieved objects based on their relevance to the query. This process enhances the quality of the final retrieved set. Here’s how it works:</p> <ul> <li><strong>Overfetch</strong>: Initially retrieve more objects than needed.</li> <li><strong>Apply Ranker Model</strong>: Use a high-latency, heavy model, typically a cross encoder, to re-evaluate the relevance of each retrieved object. This model considers the query and each retrieved object pairwise to reassess their similarity.</li> <li><strong>Reorder Results</strong>: Based on the new assessment, reorder the objects to reflect the most relevant results at the top.</li> </ul> <p>This method ensures that the most relevant documents are prioritized, improving the overall quality of the data provided to the LLM.</p> <p><img src="/assets/img/posts/2024-07-25-advanced-rag/image11.png" alt="Rerank"/> <a href="https://docs.weaviate.io/weaviate/search/rerank">AutoCut</a></p> <h3 id="3-fine-tuning-the-llm">3. Fine-Tuning the LLM</h3> <p>Fine-tuning the LLM on domain-specific data can significantly enhance its performance within that domain. For instance, using a model like Meditron 70B, which is a fine-tuned version of LLaMA 2 70b specifically for medical data using both:</p> <ul> <li><strong>Unsupervised Fine-Tuning</strong>: Continue pre-training the model on a large corpus of domain-specific text (e.g., PubMed literature).</li> <li><strong>Supervised Fine-Tuning</strong>: Further refine the model using supervised learning on domain-specific tasks (e.g., medical multiple-choice questions).</li> </ul> <p>This specialized training helps the model perform exceptionally well in the targeted domain, outperforming both its base model and larger, less specialized models like GPT-3.5 in specific tasks.</p> <h2 id="summary">Summary</h2> <p>Improving retrieval and generation in Retrieval-Augmented Generation (RAG) systems is crucial for enhancing the performance and reliability of AI applications.</p> <p>The techniques discussed above range from low-investment, high-return methods like query rewriting and reranking, to more intensive processes like embedding and LLM fine-tuning. The choice of technique depends on the specific requirements and constraints of your application.</p> <p>To make these techniques accessible, we’ve developed Verba, an open-source chatbot that integrates many of these improvements. Check it out on <a href="https://github.com/weaviate/Verba">GitHub</a> and try it out at <a href="https://verba.weaviate.io/">verba.weaviate.io</a> to see how it works with all of our documentation indexed into it. You can take this further by customizing and indexing with your data. You can also checkout the latest and greatest with Verba in the video below!</p> <iframe width="560" height="315" src="https://www.youtube.com/embed/swKKRdLBhas?si=m08JIUcgqC3sLdCr" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen=""></iframe> <p>import WhatNext from ‘/_includes/what-next.mdx’;</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="how-to"/><category term="concepts"/><summary type="html"><![CDATA[Learn how to improve the individual indexing, retreival and generation parts of your RAG pipeline!]]></summary></entry><entry><title type="html">OpenAI’s Matryoshka Embeddings in Weaviate</title><link href="https://zainhas.github.io/blog/2024/openais-matryoshka-embeddings/" rel="alternate" type="text/html" title="OpenAI’s Matryoshka Embeddings in Weaviate"/><published>2024-06-18T00:00:00+00:00</published><updated>2024-06-18T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2024/openais-matryoshka-embeddings</id><content type="html" xml:base="https://zainhas.github.io/blog/2024/openais-matryoshka-embeddings/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/hero-480.webp 480w,/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/hero-800.webp 800w,/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="OpenAI's Matryoshka Embeddings in Weaviate" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>import mrlvid from ‘/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/MRL_vectors_video.mp4’;</p> <p>In <a href="https://openai.com/index/new-embedding-models-and-api-updates/">January, OpenAI released</a> two new embedding models: <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code> and <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code>. These models are trained with the <a href="https://arxiv.org/abs/2205.13147">Matryoshka Representation Learning</a> technique, which lets developers trade off performance and cost in embeddings.</p> <p>Since then, we’ve discussed Matryoshka embeddings in various formats, such as in our 89th Weaviate podcast episode, <a href="https://youtu.be/-0m2dZJ6zos?si=dGO_jIfrKPAqHo-W">Matryoshka Embeddings with Aditya Kusupati, Zach Nussbaum, and Zain Hasan</a>, and in our <a href="https://weaviate.io/papers/paper21">Weaviate paper review, Matryoshka Representation Learning</a>.</p> <p>This article briefly covers the fundamentals of Matryoshka Representation Learning and shows how to use the newly released OpenAI’s Matryoshka embedding models with the Weaviate vector database.</p> <h2 id="what-is-matryoshka-representation-learning">What is Matryoshka Representation Learning?</h2> <p><a href="https://arxiv.org/abs/2205.13147">Matryoshka Representation Learning</a> is a technique used in training embedding models. It allows you to trade off a small amount of accuracy in exchange for much smaller embedding sizes. Thus, you can store more information at a lower cost and search for it faster.</p> <p>The embeddings are shortened by removing dimensions from the end of the sequence and only using a subset of the dimensions of the embedding vector. For example, you can only use the first 8, 16, 32, etc. dimensions (or any other slice of dimensions) of a vector that originally had 1536 dimensions.</p> <p>In contrast to common vector embeddings, where all dimensions are equally important, in Matryoshka embeddings, earlier dimensions store more information than dimensions later on in the vector, which simply adds more details. You can think of this by the analogy of trying to classify an image at multiple resolutions: The lower resolutions give more high-level information, while the higher resolutions add more details.</p> <p>Thus, the retrieval performance increases with increasing representation size. However, <a href="https://platform.openai.com/docs/guides/embeddings/">OpenAI reported</a> that a <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code> embedding can be shortened to a size of 256 while still outperforming an unshortened <code class="language-plaintext highlighter-rouge">text-embedding-ada-002</code> embedding with a size of 1536 on the MTEB benchmark.</p> <p><img src="/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/Matryoshka_representation_learning.png" alt="Matryoshka representation learning.png"/></p> <p>Retrieval performance over representation size for Matryoshka embeddings. (This image is based on Figure 7 in the <a href="https://arxiv.org/abs/2205.13147">original Matryoshka Representation Learning paper.</a>)</p> <p>The magic behind how MRL is carried out is all in the loss function which is optimized when training these models! If previously the loss function was <strong>L</strong>, for MRL we break down the loss function into the sum of the losses on individual vector dimension ranges: <code class="language-plaintext highlighter-rouge">Loss_Total = L(upto 8d) + L(upto 16d) + L(upto 32d) + ... + L(upto 2048d)</code>. With this new nested loss function there is an incentive for the model to capture information in each sub-section of the vector.</p> <p><img src="/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/Matryoshka_formula.png" alt="Matryoshka representation learning formula"/></p> <p>After modifying the loss you get these truncatable vectors for free wihtout any additional costs - this works on almost all loss functions and pre-existing models can be finetuned to output MRL vectors! This means that MRL is very easy to adopt and apply to pretrained models.</p> <h2 id="detailed-analysis-of-matryoshka-embeddings">Detailed Analysis of Matryoshka Embeddings</h2> <p>Below we will work with a <a href="https://huggingface.co/datasets/Qdrant/dbpedia-entities-openai3-text-embedding-3-large-3072-1M">dataset</a> of 1 Million objects from DBpedia that have been embedded using both the new OpenAI matryoshka <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code> model aswell as the old <code class="language-plaintext highlighter-rouge">ada-002</code> model. This will give us an understanding of how information is disproportionately stored in the the matryoshka vectors compared to thier non-matryoshka predecessors.</p> <p>In this analysis below we sampled 10k random vectors from the dataset above and plotted out the standard deviation of values seen for each dimension. The variance or standard deviation seen across a dimension is a good proxy to measure how much information that dimensions carries relative to other dimensions.</p> <table> <thead> <tr> <th style="text-align: center">Matryoshka Vectors</th> <th style="text-align: center">Non-Matryoshka Vectors</th> </tr> </thead> <tbody> <tr> <td style="text-align: center"><img src="/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/MRLvar.png" alt="MRL"/></td> <td style="text-align: center"><img src="/assets/img/posts/2024-06-18-openais-matryoshka-embeddings/OLDvar.png" alt="OLD"/></td> </tr> </tbody> </table> <p>This graphs shows the smoothed standard deviation per dimension across 10k random samples from DBpedia embedded with the new <code class="language-plaintext highlighter-rouge">text-emb3-large</code> model and the old <code class="language-plaintext highlighter-rouge">ada-002</code> model.</p> <p>We can see the standard deviation is a multi-step function of the dimension index - earlier dimensions capture more information and those later on capture less. The graph also tells us that the OpenAI embedding model was trained with 4 aggregated loss functions at dimensions = {512d, 1024d, 1536d, 3072d} in the MRL style detailed above.</p> <p>What’s more interesting is that the information within the steps(512d, 1024d, 1536d, 3072d) diffuses more or less evenly amongst the dimensions. This also explains the magical finding in the MRL paper that shows you can actually take dimensions between these pre-specified chunks (eg. 750d) and still get incremental benefits.</p> <h3 id="visualizing-a-matryoshka-vector-space">Visualizing a Matryoshka Vector Space</h3> <p>Another interesting question we can ask is: How does the underlying data representation/vector-space change as you use more and more dimensions of a Matryoshka embedding?</p> <p>In this video below every frame is a 3D vector space that was generated using principal components analysis(PCA) on upto only a certain number of MRL vector dimensions.</p> <p>We took 10k random samples, embedded using the OpenAI <code class="language-plaintext highlighter-rouge">text-embed-3-large</code> model, from the same dataset linked above and then reduced them down to 3D space using PCA; we started off using only 4-dims all the way up to using all 3072-dims.</p> <figure> <video width="100%" autoplay="" loop="" muted="" controls=""> &lt;source src={mrlvid} type="video/mp4" /&gt; Your browser does not support the video tag. </video> <figcaption>Visualizing Matryoshka vectors with increasingly more dimensions.</figcaption> </figure> <p>You can see that by the time we get to using 512 dims, the structure of the vector space is already well defined, after these inital 512 dimensions the others are mainly used to tighten data representation inside the larger structure.</p> <p>By the time we get to dims 2000+ we only see small jittering of the data points, probably because the contributions of the incremental dimensions are negligible and can’t be seen in the first 3 principal components.</p> <h2 id="getting-familiar-with-openais-matryoshka-embeddings">Getting Familiar with OpenAI’s Matryoshka Embeddings</h2> <p>OpenAI has released two new models, <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code> and <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code>, which are trained using the <a href="https://aniketrege.github.io/blog/2024/mrl/">Matryoshka Representation Learning</a> technique. The API of these models supports a <code class="language-plaintext highlighter-rouge">dimensions</code> parameter with which you can control the length of the resulting embedding.</p> <p>If you embed the same text with the same model but with a different <code class="language-plaintext highlighter-rouge">dimensions</code> parameter, you can see that the shorter embedding is not the truncated version of the longer one.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># !pip install openai
</span><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>

<span class="n">openai</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">vectorize</span><span class="p">(</span><span class="n">text</span><span class="p">,</span>
              <span class="n">dimensions</span><span class="p">,</span>
              <span class="n">model</span> <span class="o">=</span> <span class="sh">"</span><span class="s">text-embedding-3-small</span><span class="sh">"</span><span class="p">):</span>
   <span class="n">text</span> <span class="o">=</span> <span class="n">text</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span>

   <span class="k">return</span> <span class="n">openai</span><span class="p">.</span><span class="n">embeddings</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="nb">input</span> <span class="o">=</span> <span class="p">[</span><span class="n">text</span><span class="p">],</span> 
                                   <span class="n">model</span> <span class="o">=</span> <span class="n">model</span><span class="p">,</span> 
                                   <span class="n">dimensions</span> <span class="o">=</span> <span class="n">dimensions</span><span class="p">).</span><span class="n">data</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">embedding</span>

<span class="n">example_text</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Learned representations are a central component in modern ML systems, serving a multitude of downstream tasks.</span><span class="sh">"</span>

<span class="n">full</span> <span class="o">=</span> <span class="nf">vectorize</span><span class="p">(</span><span class="n">example_text</span><span class="p">,</span> <span class="n">dimensions</span> <span class="o">=</span> <span class="mi">1536</span><span class="p">)</span>
<span class="n">short</span> <span class="o">=</span> <span class="nf">vectorize</span><span class="p">(</span><span class="n">example_text</span><span class="p">,</span> <span class="n">dimensions</span> <span class="o">=</span> <span class="mi">8</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="n">full</span><span class="p">[:</span><span class="mi">8</span><span class="p">])</span>
<span class="nf">print</span><span class="p">(</span><span class="n">short</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[-0.001463836757466197, -0.0241670124232769,  0.00683123804628849, -0.013936602510511875, 0.0320618636906147, 0.00872271228581667, 0.031053075566887856, 0.021820487454533577]
[-0.025210261344909668, -0.41620534658432007, 0.11764788627624512, -0.24001678824424744,  0.5521708130836487, 0.15022294223308563, 0.5347974300384521,   0.3757933974266052]
</code></pre></div></div> <p>However, if you examine their cosine similarity, you can see that they are very similar (or even equal due to rounding).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.metrics.pairwise</span> <span class="kn">import</span> <span class="n">cosine_similarity</span>

<span class="nf">cosine_similarity</span><span class="p">([</span><span class="n">full</span><span class="p">[:</span><span class="mi">8</span><span class="p">]],</span> <span class="p">[</span><span class="n">short</span><span class="p">])</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>array([[1.]])
</code></pre></div></div> <p>If you take an even closer look, you can see that the embeddings actually only differ by a scaling factor (of 0.058 in this case).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">scale</span> <span class="o">=</span> <span class="n">full</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">/</span><span class="n">short</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
<span class="nf">print</span><span class="p">(</span><span class="n">scale</span><span class="p">)</span>
<span class="nf">print</span><span class="p">([</span><span class="n">x</span> <span class="o">*</span> <span class="n">scale</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">short</span><span class="p">])</span>
<span class="nf">print</span><span class="p">(</span><span class="n">full</span><span class="p">[:</span><span class="mi">8</span><span class="p">])</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0.05806511632065122
[-0.001463836757466197, -0.0241670118626955, 0.006831238201508919, -0.01393660272831134, 0.03206186249057062, 0.008722712614794586, 0.031053074983168057, 0.021820487334108546]
[-0.001463836757466197, -0.0241670124232769, 0.00683123804628849, -0.013936602510511875, 0.0320618636906147, 0.00872271228581667, 0.031053075566887856, 0.021820487454533577]
</code></pre></div></div> <p>Note that <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code> and <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code> differ in the length of the default embedding length:</p> <ul> <li>Default dimension for <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code>: 1536</li> <li>Default dimension for <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code>: 3072</li> </ul> <p>Additionally, they do <strong>not</strong> produce compatible embeddings when sliced to the same size:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">large</span> <span class="o">=</span> <span class="nf">vectorize</span><span class="p">(</span><span class="n">example_text</span><span class="p">,</span> <span class="n">dimensions</span> <span class="o">=</span> <span class="mi">3072</span><span class="p">,</span> <span class="n">model</span> <span class="o">=</span> <span class="sh">"</span><span class="s">text-embedding-3-large</span><span class="sh">"</span><span class="p">)</span>
<span class="n">small</span> <span class="o">=</span> <span class="nf">vectorize</span><span class="p">(</span><span class="n">example_text</span><span class="p">,</span> <span class="n">dimensions</span> <span class="o">=</span> <span class="mi">1536</span><span class="p">,</span> <span class="n">model</span> <span class="o">=</span> <span class="sh">"</span><span class="s">text-embedding-3-small</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">large</span><span class="p">[:</span><span class="mi">1536</span><span class="p">])</span>
<span class="nf">print</span><span class="p">(</span><span class="n">small</span><span class="p">)</span>

<span class="nf">cosine_similarity</span><span class="p">([</span><span class="n">large</span><span class="p">[:</span><span class="mi">1536</span><span class="p">]],</span> <span class="p">[</span><span class="n">small</span><span class="p">])</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[0.011070899665355682,   0.014488349668681622, -0.021118611097335815, -0.011152755469083786, 0.011555208824574947, -0.0007622754783369601, ... ]
[-0.001463836757466197, -0.0241670124232769,    0.00683123804628849,  -0.013936602510511875, 0.0320618636906147,    0.00872271228581667,   ... ]
array([[-0.00149749]])
</code></pre></div></div> <p>Note that if you need to change the embedding dimension after you generate it, you need to normalize the embedding dimensions. The <a href="https://www.notion.so/OpenAI-s-Matryoshka-Embeddings-in-Weaviate-6d6817039caa4012a4958ce1a0434670?pvs=21">OpenAI documentation</a> explains this more fully.</p> <h2 id="example-how-to-use-matryoshka-embeddings-in-weaviate">Example: How to Use Matryoshka Embeddings in Weaviate</h2> <p>This section quickly guides you through the steps to use OpenAI’s Matryoshka embeddings with the Weaviate vector database.</p> <p>First you need to pip install the relevant Python packages.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pip</span> <span class="n">install</span> <span class="o">-</span><span class="n">U</span> <span class="n">weaviate</span><span class="o">-</span><span class="n">client</span>
</code></pre></div></div> <p>Next, you need to setup a Weaviate vector database instance. For this example, you can <a href="https://docs.weaviate.io/weaviate/installation/docker-compose">start up a local instance of Weaviate via Docker</a>, as follows:</p> <div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">docker</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">8080:8080</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">50051:50051</span><span class="w"> </span><span class="nx">cr.weaviate.io/semitechnologies/weaviate:1.25.0</span><span class="w">
</span></code></pre></div></div> <p>Once your vector database instance is up and running, you can connect to it as shown below:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">weaviate</span>

<span class="n">client</span> <span class="o">=</span> <span class="n">weaviate</span><span class="p">.</span><span class="nf">connect_to_local</span><span class="p">()</span>
</code></pre></div></div> <h3 id="with-default-dimensions">With default dimensions</h3> <p>As the OpenAI models are tightly integrated with Weaviate, you can use them through the <code class="language-plaintext highlighter-rouge">text2vec_openai</code> module, which enabled automatic vectorization of data at import and query time. You only need to define the model name and the dimensions. The <code class="language-plaintext highlighter-rouge">text2vec_openai</code> module has some pre-defined dimensions. Available default dimensions for these models in Weaviate can be found <a href="https://docs.weaviate.io/weaviate/model-providers/openai/embeddings#available-models">in our documentation</a> and are:</p> <ul> <li><code class="language-plaintext highlighter-rouge">text-embedding-3-small</code>: 512, 1536</li> <li><code class="language-plaintext highlighter-rouge">text-embedding-3-large</code>: 256, 1024, 3072</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">weaviate.classes.config</span> <span class="kn">import</span> <span class="n">Property</span><span class="p">,</span> <span class="n">DataType</span>

<span class="c1"># Define schema and create collection
</span><span class="n">my_collection</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">collections</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">MyCollection</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">vectorizer_config</span><span class="o">=</span><span class="n">wvc</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">Configure</span><span class="p">.</span><span class="n">Vectorizer</span><span class="p">.</span><span class="nf">text2vec_openai</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">'</span><span class="s">text-embedding-3-small</span><span class="sh">'</span><span class="p">,</span>
                                                                      <span class="n">dimensions</span> <span class="o">=</span> <span class="mi">512</span><span class="p">,</span>
                                                                      <span class="n">vectorize_collection_name</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
<span class="p">),</span>
    <span class="n">properties</span><span class="o">=</span><span class="p">[</span>
        <span class="nc">Property</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span> 
                 <span class="n">data_type</span><span class="o">=</span><span class="n">DataType</span><span class="p">.</span><span class="n">TEXT</span><span class="p">,</span>
                 <span class="n">vectorize_property_name</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
<span class="p">),</span>
    <span class="p">]</span>
<span class="p">)</span>

<span class="c1"># Insert single data object
</span><span class="n">my_collection</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">insert</span><span class="p">({</span>
    <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="n">example_text</span><span class="p">,</span>
<span class="p">})</span>
</code></pre></div></div> <h3 id="with-custom-dimensions">With custom dimensions</h3> <p>If you want to use Matryoshka embeddings with a different value for the <code class="language-plaintext highlighter-rouge">dimensions</code> parameter, you will need to manually generate the embeddings at import and query time.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>
<span class="kn">from</span> <span class="n">weaviate.classes.config</span> <span class="kn">import</span> <span class="n">Property</span><span class="p">,</span> <span class="n">DataType</span>

<span class="n">openai</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">()</span>

<span class="c1"># Define function to embed text
</span><span class="k">def</span> <span class="nf">vectorize</span><span class="p">(</span><span class="n">text</span><span class="p">,</span>
              <span class="n">dimensions</span><span class="p">,</span>
              <span class="n">model</span> <span class="o">=</span> <span class="sh">"</span><span class="s">text-embedding-3-small</span><span class="sh">"</span><span class="p">):</span>
   <span class="n">text</span> <span class="o">=</span> <span class="n">text</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span>

   <span class="k">return</span> <span class="n">openai</span><span class="p">.</span><span class="n">embeddings</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="nb">input</span> <span class="o">=</span> <span class="p">[</span><span class="n">text</span><span class="p">],</span> 
                                   <span class="n">model</span> <span class="o">=</span> <span class="n">model</span><span class="p">,</span> 
                                   <span class="n">dimensions</span> <span class="o">=</span> <span class="n">dimensions</span><span class="p">).</span><span class="n">data</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">embedding</span>

<span class="c1"># Define schema and create collection
</span><span class="n">my_collection</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">collections</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">MyCollection</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">properties</span><span class="o">=</span><span class="p">[</span>
        <span class="nc">Property</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span> <span class="n">data_type</span><span class="o">=</span><span class="n">DataType</span><span class="p">.</span><span class="n">TEXT</span><span class="p">),</span>
    <span class="p">]</span>
<span class="p">)</span>

<span class="c1"># Insert single data object
</span><span class="n">my_collection</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">insert</span><span class="p">(</span>
        <span class="n">properties</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span> <span class="p">:</span> <span class="n">example_text</span><span class="p">},</span>
        <span class="n">vector</span> <span class="o">=</span> <span class="nf">vectorize</span><span class="p">(</span><span class="n">example_text</span><span class="p">,</span> <span class="n">dimensions</span><span class="o">=</span><span class="mi">128</span><span class="p">),</span>
    <span class="p">)</span>
</code></pre></div></div> <h2 id="summary">Summary</h2> <p>This article introduced you to the Matryoshka Representation Learning technique, used to shorten embeddings, and showcased how you can use them in the Weaviate vector database.</p> <p>You can learn more about Matryoshka embeddings in our <a href="https://weaviate.io/papers/paper21">Weaviate paper review on Matryoshka Representation Learning</a> by <a href="https://twitter.com/ZainHasan6">Zain</a> or in our 89th Weaviate podcast episode <a href="https://youtu.be/-0m2dZJ6zos?si=dGO_jIfrKPAqHo-W">Matryoshka Embeddings with Aditya Kusupati, Zach Nussbaum, and Zain Hasan</a> with <a href="https://twitter.com/CShorten30">Connor</a>.</p> <iframe width="560" height="315" src="https://www.youtube.com/embed/-0m2dZJ6zos?si=sy8s6Vv_nQRGm9ml" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen=""></iframe> <p>import WhatNext from ‘/_includes/what-next.mdx’;</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="how-to"/><category term="concepts"/><summary type="html"><![CDATA[How to use OpenAI's embedding models trained with Matryoshka Representation Learning in a vector database like Weaviate]]></summary></entry><entry><title type="html">Step-by-Step Guide to Choosing the Best Embedding Model for Your Application</title><link href="https://zainhas.github.io/blog/2024/how-to-choose-an-embedding-model/" rel="alternate" type="text/html" title="Step-by-Step Guide to Choosing the Best Embedding Model for Your Application"/><published>2024-06-04T00:00:00+00:00</published><updated>2024-06-04T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2024/how-to-choose-an-embedding-model</id><content type="html" xml:base="https://zainhas.github.io/blog/2024/how-to-choose-an-embedding-model/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-06-04-how-to-choose-an-embedding-model/hero-480.webp 480w,/assets/img/posts/2024-06-04-how-to-choose-an-embedding-model/hero-800.webp 800w,/assets/img/posts/2024-06-04-how-to-choose-an-embedding-model/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2024-06-04-how-to-choose-an-embedding-model/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Step-by-Step Guide to Choosing the Best Embedding Model for Your Application" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><a href="/blog/vector-embeddings-explained">Vector embeddings</a> power modern search and Retrieval-Augmented Generation (RAG) applications. Embeddings capture the semantic meaning of data objects (e.g., text) and represent them in an array of numbers. In today’s Generative AI applications, these vector embeddings are typically generated with so-called embedding models.</p> <p><img src="/assets/img/posts/2024-06-04-how-to-choose-an-embedding-model/embedding_model.png" alt="embedding model.png"/></p> <p>As a <a href="/blog/what-is-a-vector-database">vector database</a> that stores vector embeddings and retrieves data objects based on vector search, Weaviate has many <a href="https://docs.weaviate.io/weaviate/model-providers">integrations with various model providers and their wide variety of embedding models</a>.</p> <p>But how do you approach selecting the right embedding model for your search or RAG application? As you will learn in this article, it depends on your use case and specific requirements.</p> <h2 id="step-1-identify-your-use-case">Step 1: Identify your use case</h2> <p>Would a general-purpose model be sufficient for what you are trying to achieve, or do you have specific needs, such as modality (e.g., text only or multimodal), subject domain (e.g., coding, law, medical, multilingual, etc.), and deployment mode? In most cases, starting with a general-purpose model for your desired modalities would be a sensible baseline.</p> <h2 id="step-2-select-a-baseline-model">Step 2: Select a baseline model</h2> <p>The <a href="https://huggingface.co/spaces/mteb/leaderboard">Massive Text Embedding Benchmark (MTEB) Leaderboard</a> is a good starting point for getting an overview of the current landscape of the wide range of proprietary and open source text embedding models. For each embedding model, the MTEB lists various metrics, such as the model size, memory usage, embedding dimensions, maximum number of tokens, and its score for tasks such as retrieval, summarization, etc.</p> <p>Here are a few considerations for choosing the best model for your application:</p> <ul> <li><strong>Task:</strong> On the top of the MTEB Leaderboard, you will see various tabs. You first have to decide whether you want an all-rounder or if you can find a task (classification, clustering, retrieval, summarization, etc.) that matches your specific use case. For example, if you are building an RAG application, you might want to look closer at the “Retrieval” task. You might also want to refine your selection if you have more specific needs, such as a specific language (e.g., English, Chinese, French, Polish) or domain (e.g., law).</li> <li><strong>Score</strong>: This will show you how well the model performs on a specific benchmark dataset or across multiple benchmark datasets. Depending on the task, different <a href="/blog/retrieval-evaluation-metrics/">evaluation metrics</a> are used. Usually, these metrics can take values between 0 and 1, where higher values indicate a better performance.</li> <li><strong>Model size and memory usage:</strong> These give you an idea of the computational resources required to run the model. While retrieval performance scales with model size, it is important to note that model size also directly impacts latency. Additionally, it is worth mentioning that larger models can also overfit and thus underperform in production. Thus, you want to aim for a good latency-performance trade-off for a production setup. Ideally, start with a small, lightweight model to be able to build a baseline upon which you can iterate quickly. Once your pipeline works, you can switch out the model with a bigger and more performant one later.</li> <li><strong>Embedding dimensions</strong>: This is the length of the embedding vector. While larger embedding dimensions can capture more nuanced details and relationships in the data, they aren’t necessarily always better. Do you really need 2048 dimensions to chat with a PDF? Probably not. On the other hand, smaller embedding dimensions offer faster inference and are more storage- and memory-efficient. Thus, you will want to aim for a good trade-off between capturing the complexity of data and operational efficiency.</li> <li><strong>Max tokens</strong>: This is the maximum number of tokens that can be converted into a single embedding. For common RAG applications, a good chunk size for an embedding is typically about a single paragraph of text or less. In this case, models with max tokens of 512 should be sufficient. However, there will be special cases where you need to embed longer source texts that require models with a larger context window.</li> </ul> <p><img src="/assets/img/posts/2024-06-04-how-to-choose-an-embedding-model/mteb.png" alt="mteb leaderboard"/></p> <h2 id="step-3-evaluate-the-model-on-your-use-case">Step 3: Evaluate the model on your use case</h2> <p>While the MTEB Leaderboard is a great place to start, you should take its results cautiously and skeptically. Bear in mind that these results are self-reported. It is also possible that some models produce inflated performance scores, as they may include the MTEB datasets in their training data since they are publicly available. Finally, the datasets the models are benchmarked on might not accurately represent the data you are dealing with. Thus, we recommend that you evaluate the embedding model on your own dataset, as we will show you below. You can find the related code in our <a href="https://github.com/weaviate/recipes/blob/main/weaviate-features/evaluation/how_to_choose_an_embedding_model.ipynb">recipes GitHub repository</a>.</p> <h3 id="prepare-dataset">Prepare dataset</h3> <p>For this, you could generate a small, hand-labeled dataset with 50 to 100 data objects and see what performance you can achieve. We will create a minimal dataset with eight different pastries to follow the example used in <a href="https://youtu.be/lpdN3aw-yTg?si=6-gC0ql-m0KMf3Qh">Zain and JP’s discussion</a> and our <a href="/blog/retrieval-evaluation-metrics/">previous blog post on evaluation metrics</a>. You can find the .csv file in the <a href="https://github.com/weaviate/recipes/blob/main/weaviate-features/evaluation">related GitHub repository</a>.</p> <table> <thead> <tr> <th>pastry_id</th> <th>pastry_name</th> <th>pastry_description</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>Bagel</td> <td>A classic round bread with a chewy interior and a golden crust, perfect for toasting or topping with cream cheese, smoked salmon, or your favorite spreads.</td> </tr> <tr> <td>2</td> <td>Roll</td> <td>Soft and pillowy, these rolls are versatile companions to any meal. Enjoy them fresh out of the oven or sliced for sandwiches, filled with your choice of meats, cheeses, and veggies.</td> </tr> <tr> <td>3</td> <td>Donut</td> <td>Indulge in these sweet, fried delights. Glazed, powdered, or filled with decadent creams and fruit jams, each bite is a delightful burst of flavor and nostalgia.</td> </tr> <tr> <td>4</td> <td>Muffin</td> <td>Moist and tender, these muffins are bursting with flavor. Whether you prefer classic blueberry, indulgent chocolate chip, or hearty bran, there’s a muffin for every craving.</td> </tr> <tr> <td>5</td> <td>Croissant</td> <td>Buttery layers of flaky pastry are folded to perfection, creating a delicate and irresistible treat. Enjoy them plain, stuffed with savory fillings, or paired with your favorite coffee.</td> </tr> <tr> <td>6</td> <td>Scone</td> <td>Crumbly yet tender, these scones are the epitome of comfort. Enjoy them plain or studded with fruits, nuts, or chocolate chips, accompanied by a dollop of clotted cream and jam.</td> </tr> <tr> <td>7</td> <td>Pretzel</td> <td>Crunchy on the outside, soft on the inside, these pretzels are a salty snack lover’s dream. Enjoy them twisted into traditional shapes or dipped in sweet or savory toppings for a delightful twist.</td> </tr> <tr> <td>8</td> <td>Sandwich</td> <td>Freshly baked bread is the foundation for these hearty sandwiches. Pile on layers of meats, cheeses, crisp vegetables, and flavorful spreads for a satisfying meal on the go.</td> </tr> </tbody> </table> <h3 id="create-embeddings">Create embeddings</h3> <p>Now, let’s generate and store the corresponding vector embeddings in our Weaviate vector database. For this, you will first connect to a Weaviate client in <a href="https://docs.weaviate.io/weaviate/installation/embedded">embedded mode</a> for simplicity (<a href="https://docs.weaviate.io/weaviate/installation">but you can instantiate a Weaviate client locally or use our managed cloud service</a>).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">weaviate</span>
<span class="kn">import</span> <span class="n">os</span>

<span class="c1"># Connect to Weaviate client in embedded mode for simplicity
</span><span class="n">client</span> <span class="o">=</span> <span class="n">weaviate</span><span class="p">.</span><span class="nf">connect_to_embedded</span><span class="p">(</span>
	<span class="n">headers</span> <span class="o">=</span> <span class="p">{</span>
		<span class="sh">"</span><span class="s">X-OpenAI-Api-Key</span><span class="sh">"</span><span class="p">:</span> <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">OPENAI_API_KEY</span><span class="sh">"</span><span class="p">]</span>
		<span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div> <p>Then, you will configure a data collection called <code class="language-plaintext highlighter-rouge">"Pastries"</code> and configure the properties and vectorizer. This example uses OpenAI’s <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code> embedding model to vectorize the data at import and query time automatically. Note, that we will repeat this step later with <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code> to compare the two models. For this, you might want to have a look at <a href="https://docs.weaviate.io/weaviate/manage-data/delete">how to delete a collection</a>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">weaviate.classes</span> <span class="k">as</span> <span class="n">wvc</span>
<span class="kn">from</span> <span class="n">weaviate.classes.config</span> <span class="kn">import</span> <span class="n">Property</span><span class="p">,</span> <span class="n">DataType</span>

<span class="c1"># Create data collection
</span><span class="n">pastries</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">collections</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Pastries</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">vectorizer_config</span><span class="o">=</span><span class="n">wvc</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">Configure</span><span class="p">.</span><span class="n">Vectorizer</span><span class="p">.</span><span class="nf">text2vec_openai</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">'</span><span class="s">text-embedding-3-small</span><span class="sh">'</span><span class="p">),</span>
    <span class="n">properties</span><span class="o">=</span><span class="p">[</span>
        <span class="nc">Property</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">pastry_name</span><span class="sh">"</span><span class="p">,</span> <span class="n">data_type</span><span class="o">=</span><span class="n">DataType</span><span class="p">.</span><span class="n">TEXT</span><span class="p">),</span>
        <span class="nc">Property</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">pastry_description</span><span class="sh">"</span><span class="p">,</span> <span class="n">data_type</span><span class="o">=</span><span class="n">DataType</span><span class="p">.</span><span class="n">TEXT</span><span class="p">),</span>
    <span class="p">]</span>
<span class="p">)</span>
</code></pre></div></div> <p>Finally, you can import your data:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Import data objects
</span><span class="n">pastry_objects</span> <span class="o">=</span> <span class="nf">list</span><span class="p">()</span>
<span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">row</span> <span class="ow">in</span> <span class="n">df</span><span class="p">.</span><span class="nf">iterrows</span><span class="p">():</span>
    <span class="n">properties</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">pastry_name</span><span class="sh">"</span><span class="p">:</span> <span class="n">row</span><span class="p">.</span><span class="n">pastry_name</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">pastry_description</span><span class="sh">"</span><span class="p">:</span> <span class="n">row</span><span class="p">.</span><span class="n">pastry_description</span>
    <span class="p">}</span>
    <span class="n">pastry_objects</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">properties</span><span class="p">)</span>

<span class="n">pastries</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">insert_many</span><span class="p">(</span><span class="n">pastry_objects</span><span class="p">)</span>
</code></pre></div></div> <h3 id="retrieve">Retrieve</h3> <p>Now, you can run a few queries:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">weaviate.classes.query</span> <span class="kn">import</span> <span class="n">MetadataQuery</span>

<span class="n">response</span> <span class="o">=</span> <span class="n">pastries</span><span class="p">.</span><span class="n">query</span><span class="p">.</span><span class="nf">near_text</span><span class="p">(</span>
    <span class="n">query</span><span class="o">=</span><span class="sh">"</span><span class="s">Sweet pastry</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">limit</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span>
    <span class="n">return_metadata</span><span class="o">=</span><span class="nc">MetadataQuery</span><span class="p">(</span><span class="n">distance</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="p">)</span>
</code></pre></div></div> <h3 id="evaluate-performance">Evaluate performance</h3> <p>This section compares two OpenAI embedding models, <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code> and <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code>, for three sample queries. Note that the desired outputs for each query are highly subjective. But this is exactly the point: You must decide what result you want for your use case.</p> <p>Various <a href="/blog/retrieval-evaluation-metrics/">evaluation metrics</a> are available, such as precision, recall, MRR, MAP, and NDCG. In this section, we will use precision and recall as evaluation metrics.</p> <p><strong>Query 1: “Sweet pastry”</strong></p> <p>Relevant items: Donut, Muffin, Scone (Note, that this list is subjective because we have decided what will be relevant for this query)</p> <p>Results:</p> <table> <thead> <tr> <th>Rank</th> <th>text-embedding-3-small</th> <th>text-embedding-3-large</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>✅Donut</td> <td>❌Croissant</td> </tr> <tr> <td>2</td> <td>❌Croissant</td> <td>✅Donut</td> </tr> <tr> <td>3</td> <td>❌Roll</td> <td>✅Scone</td> </tr> <tr> <td>4</td> <td>❌Bagel</td> <td>❌Sandwich</td> </tr> <tr> <td>Precision</td> <td>0.25</td> <td>0.5</td> </tr> <tr> <td>Recall</td> <td>0.33</td> <td>0.67</td> </tr> </tbody> </table> <p><strong>Query 2: “Suitable for lunch”</strong></p> <p>Relevant items: Sandwich, Bagel, Roll, Pretzel (Note, that this list is subjective because we have decided what will be relevant for this query)</p> <p>Results:</p> <table> <thead> <tr> <th>Rank</th> <th>text-embedding-3-small</th> <th>text-embedding-3-large</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>✅Sandwich</td> <td>✅Sandwich</td> </tr> <tr> <td>2</td> <td>✅Roll</td> <td>✅Roll</td> </tr> <tr> <td>3</td> <td>✅Bagel</td> <td>✅Bagel</td> </tr> <tr> <td>4</td> <td>❌Scone</td> <td>❌Croissant</td> </tr> <tr> <td>Precision</td> <td>0.75</td> <td>0.75</td> </tr> <tr> <td>Recall</td> <td>0.75</td> <td>0.75</td> </tr> </tbody> </table> <p><strong>Query 3: “Goes well with jam”</strong></p> <p>Relevant items: Bagel, Croissant, Roll (Note, that this list is subjective because we have decided what will be relevant for this query)</p> <p>Results:</p> <table> <thead> <tr> <th>Rank</th> <th>text-embedding-3-small</th> <th>text-embedding-3-large</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>❌Sandwich</td> <td>❌Scone</td> </tr> <tr> <td>2</td> <td>✅Bagel</td> <td>✅Bagel</td> </tr> <tr> <td>3</td> <td>❌Scone</td> <td>❌Donut</td> </tr> <tr> <td>4</td> <td>✅Roll</td> <td>❌Sandwich</td> </tr> <tr> <td>Precision</td> <td>0.5</td> <td>0.25</td> </tr> <tr> <td>Recall</td> <td>0.67</td> <td>0.33</td> </tr> </tbody> </table> <p>Across the three example queries, both <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code> and <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code> result in an average precision of 0.5 and an average recall of 0.58, although they return different results for the queries. As you can see, you would need to increase both the number of data objects in the dataset and the number of queries to evaluate the embedding models. However, this small example should give you a good starting point for building your own evaluation pipeline.</p> <h2 id="step-4-iterate">Step 4: Iterate</h2> <p>After you’ve built an initial pipeline for your use case with a small baseline model, you can experiment with a few different embedding models and see if you can perform better than your baseline choice.</p> <h2 id="optional-step-fine-tuning">Optional Step: Fine-tuning</h2> <p>Fine-tuning an embedding model is only an optional step. Ideally, you can find a suitable model off the shelf, and fine-tuning might only be necessary for the last bit of performance squeeze.</p> <h2 id="summary">Summary</h2> <p>This article is inspired by a recent discussion between <a href="https://twitter.com/ZainHasan6">Zain</a> and <a href="https://twitter.com/_jphwang">JP</a>. It discussed the steps you can take to select the right embedding model for your search or Generative AI application. You can listen to the full discussion on YouTube and find the related code in our <a href="https://github.com/weaviate/recipes/blob/main/weaviate-features/evaluation/how_to_choose_an_embedding_model.ipynb">recipes GitHub repository</a>.</p> <iframe width="560" height="315" src="https://www.youtube.com/embed/lpdN3aw-yTg?si=Wg-bHmgZK6Oa3cxz" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen=""></iframe> <p>import WhatNext from ‘/_includes/what-next.mdx’;</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="how-to"/><category term="concepts"/><summary type="html"><![CDATA[How to select an embedding model for your search and retrieval-augmented generation system.]]></summary></entry><entry><title type="html">32x Reduced Memory Usage With Binary Quantization</title><link href="https://zainhas.github.io/blog/2024/binary-quantization/" rel="alternate" type="text/html" title="32x Reduced Memory Usage With Binary Quantization"/><published>2024-04-02T00:00:00+00:00</published><updated>2024-04-02T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2024/binary-quantization</id><content type="html" xml:base="https://zainhas.github.io/blog/2024/binary-quantization/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-04-02-binary-quantization/hero-480.webp 480w,/assets/img/posts/2024-04-02-binary-quantization/hero-800.webp 800w,/assets/img/posts/2024-04-02-binary-quantization/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2024-04-02-binary-quantization/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="'32x Reduced Memory Usage With Binary Quantization'" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>:::info Overview</p> <ul> <li><a href="#what-is-binary-quantization">What is Binary Quantization?</a> - How BQ reduces the memory requirements of running Weaviate.</li> <li><a href="#details-of-working-with-binarized-vectors">Details of Working with Binarized Vectors</a> - What to consider when using binary quantization.</li> <li><a href="#-performance-improvements-with-bq">Weaviate Performance Improvements with BQ</a> - QPS vs. recall, indexing times, and memory savings.</li> <li><a href="#%EF%B8%8Fcomparing-product-and-binary-quantization">Comparing Product and Binary Quantization</a> - Experiments comparing PQ and BQ.</li> <li><a href="#benchmarking-bq-with-your-own-data">Benchmarking BQ</a> - Scripts for benchmarking BQ with your own data. :::</li> </ul> <hr/> <h2 id="what-is-binary-quantization">🧮What is Binary Quantization?</h2> <p>To perform search current vector databases build massive vector indexes and search over them in memory. This allows for real-time responses to queries however, compute costs can add up. Binary quantization(BQ) is a vector compression algorithm that allows you to trade-off between retrieval accuracy and memory requirements.</p> <p>Lets understand how this technique works by way of an analogy. Imagine every vector you want to store is a home address. The details of this home address allow you to precisely locate where someone lives including the country, state, city, street number, and even down to the house number. The price you pay for this pin-point accuracy is that each address takes up more memory to store, search and read. Similarly, to locate a vector in space you can think of each dimension as instructions on exactly how far to move along an axis. The sign of the number at that dimension tells you which direction to move and the magnitude of the number tells you how much to move in that direction.</p> <p>The process of binary quantization(BQ) takes all the numbers stored per dimension of a vector and only retains a 1(for positive values) or a 0(for negative values) capturing the sign of the corresponding number. That seems a bit radical, how can you ever locate a vector without information on how far along to move in the direction of an axis? Though BQ might sound like a crazy proposition, it does work particularly well for high-dimensional vectors. Let’s see why!</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/bq_vecs.png’).default} alt=”bq vectors” style= /&gt;</p> <p><strong>Fig</strong>: <em>Elementwise binary quantization of a vector from float32 to 1 bit.</em></p> <p>:::info Enabling Binary Quantization in Weaviate To learn how to enable and configure Weaviate to use BQ refer to the docs <a href="https://docs.weaviate.io/weaviate/configuration/compression/bq-compression">here</a>. You can read more about HNSW+BQ in the documentation <a href="https://docs.weaviate.io/weaviate/concepts/vector-quantization#binary-quantization">here</a>. :::</p> <p>Binarizing is not exclusive to embedding vectors, we can also understand it from the other domains in which it is used - for example in computer vision. Imagine you have an image. The process of binarizing that image entails looking at every pixel. If that pixel is larger than a certain threshold value, we will replace it with 1; otherwise, we will replace it with 0. The resulting image is a black-and-white binary image. This process loses image details but significantly compresses the image size.</p> <p>|Original Image | Binarized Image | |:————————-:|:————————-: | | <img src="/assets/img/posts/2024-04-02-binary-quantization/image11.png" alt="OriginalImage"/> | <img src="/assets/img/posts/2024-04-02-binary-quantization/image9.png" alt="BinaryImage"/> | <strong>Fig</strong>: <em>An image when binary quantized</em></p> <p>Now, let’s consider what the process of binarizing a vector embedding for a sentence looks like. In the figure below we have vectorized the sentence: “All your vector embeddings belong to you!” into a 384-dimensional vector. The first image shows all 384 numbers of the vector, each one being a 32-bit floating point, as color shades on a heatmap. Each vector dimension dictates the colour gradient on the heatmap. The image below shows the same vector, but we threshold the vector such that all positive valued dimensions are turned into 1(white) and negative valued dimensions are turned to 0(black). As a result, we get a black-and-white heatmap that looks kind of like a bar code. This is what the process of binary quantization looks like for vector embeddings! The resulting vectors are much smaller but also less detailed.</p> <p>|Sentence: <em>“All your vector embeddings belong to you!”</em> | |:————————-:| | <img src="/assets/img/posts/2024-04-02-binary-quantization/image14.jpg" alt="OriginalVec"/> | | <img src="/assets/img/posts/2024-04-02-binary-quantization/image7.png" alt="BinaryVec"/> | <strong>Fig</strong>: <em>Visualization of a sentence vector embedding when binary quantized</em></p> <p>The concept of binary quantization simplifies the encoding of vectors by retaining only their directionality. Each vector dimension is encoded with a single bit, indicating whether it’s positive or negative. For instance, a vector like <code class="language-plaintext highlighter-rouge">[12, 1, -100, 0.003, -0.001, 128, -1000, 0.0001]</code> would be condensed into a single byte, resulting in the binary sequence <code class="language-plaintext highlighter-rouge">[1,1,0,1,0,1,0,1]</code>. This massively reduces the amount of space every vector takes by a factor of 32x by converting the number stored at every dimension from a <code class="language-plaintext highlighter-rouge">float32</code> down to <code class="language-plaintext highlighter-rouge">1-bit</code>. However, reversing this process is impossible - making this a lossy compression technique.</p> <h2 id="details-of-working-with-binarized-vectors">📐Details of Working with Binarized Vectors</h2> <h3 id="distance-between-binary-vectors">Distance Between Binary Vectors</h3> <p>First, let’s consider how we compute the distance between two BQ compressed vectors. The solution is straightforward: since we’re solely concerned with their directional aspects, we only need to assess the agreement between their dimensions. This requires you to count the differing bits in both compressed representations. Here you can leverage bitwise operations and is significantly faster than computing the distance between non-binarized vectors.</p> <p>For example, considering the vector <code class="language-plaintext highlighter-rouge">[12, 1, -100, 0.03, -0.01, 128, -100, 0.01]</code> encoded as <code class="language-plaintext highlighter-rouge">11010101</code>, and a second vector <code class="language-plaintext highlighter-rouge">[11, 4, -99, -0.01, 0.02, 130, -150, 0.02]</code> encoded as <code class="language-plaintext highlighter-rouge">11001101</code>, the distance between them is determined by the count of differing bits, resulting in a distance of 2. This is more popularly known as the <code class="language-plaintext highlighter-rouge">hamming distance</code> between the two binary vectors.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image24.png’).default} alt=”BQ Distance” style= /&gt;</p> <h3 id="the-importance-of-data-distribution-for-bq">The importance of data distribution for BQ</h3> <p>Unlike product quantization, which we cover in a previous <a href="/blog/pq-rescoring">blog here</a>, binary quantization is not universally applicable to all types of data - we will see illustration of why this is this case in a second. However, assuming we are operating with normalized data, particularly when utilizing the cosine metric, there’s no need for concern as Weaviate handles data normalization for you seamlessly. Now, let’s discuss the implications of increasing the dimension count.</p> <p><strong>BQ with 1 Dimensional Vectors:</strong></p> <p>In the picture below, we plot the only two possible points, in red, for a normalized one-dimensional space (0, 1). The quantizer would assign the code with a single bit set to one to a positive vector and the code with a single bit set to zero to a negative vector.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image19.png’).default} alt=”1D BQ” style= /&gt;</p> <p><strong>Fig</strong>: <em>BQ with 1 dimensional vectors</em></p> <p>Let’s broaden our perspective to encompass two dimensions. Here, we anticipate all vectors lying within the circle centered at (0,0) with a radius of 1 (normalized vectors). Our focus shifts to understanding how the quantizer divides the data into four distinct regions, leveraging two codes and two dimensions to achieve the power of two.</p> <p>|Original Vectors | BQ Vectors | |:————————-:|:————————-: | | <img src="/assets/img/posts/2024-04-02-binary-quantization/image18.png" alt="OriginalVectors"/> | <img src="/assets/img/posts/2024-04-02-binary-quantization/image8.png" alt="BinaryVectors"/> | <strong>Fig</strong>: <em>BQ with 2 dimensional vectors</em></p> <p>In this context, the green region (coded as 11) aggregates points where both dimensions are positive, while the blue region (coded as 00) encompasses points where both dimensions are negative. The red region (coded as 10) represents scenarios where the first dimension is positive and the second is negative, while the yellow region (coded as 01) signifies instances where the first dimension is negative while the second is positive.</p> <p>Importantly, within each region, any point shares a zero distance with any other point within the same region. Conversely, points in adjacent regions maintain a distance of 1 from one another. When traversing to the opposite region, the maximal distance extends to 2.</p> <p>This delineation underscores the critical role of data distribution. While we’ve emphasized normalized data, it’s worth noting that while normalization isn’t mandatory, it significantly aligns with the scenario described. Contrastingly, let’s analyze another scenario.</p> <table> <thead> <tr> <th style="text-align: center">Original Vectors</th> <th style="text-align: center">BQ Vectors</th> </tr> </thead> <tbody> <tr> <td style="text-align: center"><img src="/assets/img/posts/2024-04-02-binary-quantization/image17.png" alt="OriginalVectorsUN"/></td> <td style="text-align: center"><img src="/assets/img/posts/2024-04-02-binary-quantization/image16.png" alt="BinaryVectorsUN"/></td> </tr> </tbody> </table> <p>All of our data falls within the quadrant where both dimensions are positive. Consequently, all vectors are encoded as ‘11’, making it challenging to discern between most vectors. This scenario exemplifies how the distribution of data can render binary quantization impractical. As previously noted, while normalization isn’t obligatory, opting for normalized data provides reassurance in terms of data distribution, facilitating the use of binary quantization.</p> <p>However, if your data isn’t normalized, ensuring balance and logical division of regions becomes imperative. Consider the following example.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image3.png’).default} alt=”BQ distance Calc detailed” style= /&gt;</p> <p><strong>Fig</strong>: <em>Displays some counter-intuitive facts about hamming distances between 2d binary vectors</em></p> <p>In this scenario, counterintuitively, the quantizer would indicate that yellow points are further from red points and closer to both blue and green points. While this holds in angular-based metrics like <code class="language-plaintext highlighter-rouge">cosine</code>, it contradicts the interpretation under the <code class="language-plaintext highlighter-rouge">L2</code> metric where we can see that the yellow and red points are closer in proximity.</p> <p><strong>BQ with N-Dimensional Vectors</strong></p> <p>Let’s talk about the density of your data and the ability to uniquely represent vectors after applying binary quantization. By comparing the number of dimensions to the number of data points, you can anticipate the level of collisions, we define a collision occurring between two vectors as the vectors having the same representation once binarized. As seen in previous examples, with binary quantization in two dimensions, you’re limited to constructing four regions. Consequently, when the number of vectors exceeds four, collisions occur, rendering distinction impossible.</p> <p>However, the upside lies in the exponential growth of regions with additional dimensions. With each dimension, the number of regions doubles ($2^d$), offering greater potential for differentiation between vector representations. For instance, with 756 dimensions, you already have astonishing $2^{756}$ regions at your disposal - this makes collisions between vectors highly improbable even when you have billions or trillions of vectors. With 1.5K dimensions, the number of regions can easily accommodate any practical amount of vectors without a single collision.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image4.png’).default} alt=”collision probability” style= /&gt;</p> <p><strong>Fig</strong>: <em>Displays the exponentially decreasing probability for collisions in vector representations. In essence, this means that the higher the dimensionality of your data, the more effectively you can expect binary quantization to perform.</em></p> <h2 id="-performance-improvements-with-bq">🚀 Performance Improvements with BQ</h2> <p>Let’s revisit the advantages of binary quantization. Typically, we employ quantization to conserve memory, encoding each dimension as a single bit. In Weaviate, floating-point vectors are represented as <code class="language-plaintext highlighter-rouge">float32</code> arrays, yielding a compression rate of 1:32, which is already commendable.</p> <p>However, there’s a significant secondary benefit: bitwise operations now suffice for distance calculations between compressed binary vectors. This calculation involves a straightforward exclusive OR (XOR) operation between two binary arrays, tallying the resulting ones. Moreover, Go offers SIMD-optimized operations for these binary functions, resulting in notably faster computation than with original vectors. But how much faster exactly?</p> <p>To address this inquiry, we present brute-force search results using our quantizer and the original vectors. We conduct searches across 10,000 vectors with 100 queries, across dimensions ranging from 768, 1536, to 4608.</p> <p>| <strong>Dimensions</strong> | <strong>768d</strong> | <strong>1536d</strong> | <strong>4608d</strong> | |————-|——|——-|——-| | Latency uncompressed (microseconds) | 1771.85 | 3703.68 | 16724.41 | | Latency compressed (microseconds) | 230.72 (13%) | 353.3 (9%) | 896.37 (5%)| | Recall | 0.745 | 0.744 | 0.757 | <strong>Table 1</strong>: <em>Vector search time improvements between uncompressed and BQ.</em></p> <p>While the recall isn’t exceptionally high, we tackle this concern later through over fetching candidate neighbors and rescoring results. It’s worth noting that as the vector dimension gets larger, we observe more significant speedups. For instance, when brute forcing 768-dimensional compressed vectors, we require only 13% of the time compared to using uncompressed vectors. Similarly, for 1536-dimensional compressed vectors, we need just 9% of the time, and for 4608-dimensional ones, only 5% of the time compared to uncompressed vectors.</p> <p>Traditionally, we rely on building a graph to facilitate approximate search, as manually searching through millions of vectors is impractical. However, with such significant time reductions, brute forcing your data becomes a viable option. For instance, in the case of 768 dimensions, brute forcing 1 million vectors should only take 23 milliseconds. Even in the worst-case scenario (4608 dimensions), it’s now feasible, requiring approximately 90 milliseconds.</p> <p>So, what’s the bottom line? Can Weaviate deliver lightning-fast brute-force searches for your data? The answer depends on the size of your data and your search speed expectations.</p> <p>There are several advantages to brute force searching your data. Firstly, you can bypass the need for data indexing, saving the time required to build the index. While indexing in Weaviate isn’t overly sluggish, brute forcing allows you to skip this step entirely. Secondly, you no longer need to store a proximity graph, resulting in further memory savings. In fact, if you opt to brute force search your data directly from disk, memory usage becomes negligible – a mere 100MB is sufficient to host your application.</p> <p>Weaviate recently introduced the <a href="https://docs.weaviate.io/weaviate/concepts/vector-index#flat-index">flat index</a>, offering the option to brute force data either from disk (the default behavior) or by retaining only compressed data in memory and fetching a small selection of full vectors from disk for final candidate rescoring. Both approaches expedite data ingestion compared to the traditional HNSW index while also reducing memory consumption. However, if your requirements demand high performance, HNSW remains the preferred choice. Nevertheless, the flat index presents a cost-effective, high-performing alternative. Furthermore, Weaviate now supports binary quantization (BQ) for use with both the flat and HNSW indexes.</p> <h3 id="indexing-time-improvements">Indexing Time Improvements</h3> <p>Now, let’s discuss the performance metrics. All experiments were conducted using the Go benchmark. Towards the end of this blog, we’ll provide information for those interested in replicating these experiments with their data. To kick things off, we’ll start with a modest dataset of 100,000 vectors from DBPedia, utilizing ADA002 (1536 dimensions) and Cohere v2 (4096 dimensions) embeddings, beginning with looking at indexing time.</p> <p>| <strong>Dimensions</strong> | <strong>1536</strong> | <strong>4096</strong> | |——————-|——–|——–| | Flat index | 5s | 7s | | Hnsw index | 47s | 1m36s | | Hnsw index+BQ | 21s | 25s | <strong>Table 2</strong>: <em>Indexing time improvements between flat, HNSW uncompressed and BQ indexes.</em></p> <p>As mentioned previously, the flat index eliminates the need for data indexing. Instead, we simply send the data to the server and store it in the stores. Conversely, the HSNW index necessitates data indexing. It’s worth noting that the HNSW index can also benefit significantly from this compression in terms of indexing time.</p> <h3 id="memory-footprint-improvements">Memory Footprint Improvements</h3> <p>Now, let’s discuss the memory footprint. We’ll differentiate between the available options for the flat index as they exhibit varying footprints. When using the flat index with or without BQ, all data is retrieved from disk, independent of data size. If we opt to cache the compressed data, it’s stored in memory. As observed, the footprint remains lower than that produced by HNSW+BQ, as no proximity graph resulting from the indexing process is required. Additionally, we’ll showcase the footprint for the HNSW cases. In both cases, you can anticipate the memory footprint to grow more or less linearly with both dimensions and vectors.</p> <p>| <strong>Dimensions</strong> | <strong>1536</strong> | <strong>4096</strong> | |—————————–|——–|——–| | Flat index | 77MB | 77MB | | Flat index + BQ + Cache | 141MB | 183MB | | Hnsw index | 1.02GB | 1.79GB | | Hnsw index+BQ | 214MB | 297MB | <strong>Table 3</strong>: <em>Memory footprint improvements for the various indexes.</em></p> <h3 id="latency-analysis">Latency Analysis</h3> <p>Finally, let us take a look at the QPS(queries per second) vs. Recall curves to get an idea of the solutions’ performance. To produce such curves, we modified the <code class="language-plaintext highlighter-rouge">ef</code> parameter for the case of HNSW and the <code class="language-plaintext highlighter-rouge">rescoringLimit</code> for the case of the flat index. We also used a total of 10 concurrent cores to measure the QPS.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image21.png’).default} alt=”latency” style= /&gt;</p> <p><strong>Fig</strong>: <em>QPS vs Recall for 100k DBPedia vectors</em></p> <p>Note the low QPS in the scenario of the pure flat index (depicted as the green dot at the bottom right). Here, we’re compelled to retrieve all full vectors from the disk and conduct brute-force operations over the uncompressed vectors. While this approach is notably slow, it requires zero memory allocation.</p> <p>Moving on, we employ the same process but integrate binary quantization (BQ). In this iteration, we retrieve less information from disk, as we only need to access the compressed vectors (32x smaller than their uncompressed counterparts). Moreover, brute force operations are expedited, as we operate solely with bits. We generate a candidates list and subsequently rescore them. During the rescoring process, we retrieve only a small subset of the full vectors to construct the final solution. This intermediary solution still maintains a memory-free operation while delivering significantly enhanced performance. It’s important to note that this approach hinges on BQ compatibility; otherwise, optimal recall may not be achieved. Additionally, ensuring a sufficiently high <code class="language-plaintext highlighter-rouge">rescoringLimit</code> is important to guarantee good recall.</p> <p>Lastly, we introduce the flat index with cached compressed vectors (illustrated by the blue curve). Depending on your recall expectations, you can anticipate QPS ranging from 600 to 1000. However, the memory footprint slightly increases in this scenario, as the list of compressed vectors is retained in memory, with only a small subset of full vector candidates fetched from disk.</p> <p>Next, we’ll present the corresponding results for cases involving larger dimensions.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image15.png’).default} alt=”latency 100k” style= /&gt;</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image6.png’).default} alt=”latency 100k 2” style= /&gt;</p> <p><strong>Fig</strong>: <em>QPS vs Recall for 100k DBPedia vectors comparing various indexes.</em></p> <p>Given these results it’s worth considering the following: for a relatively small set of vectors (~100,000), if you’re aiming for exceptionally high recall, the performance disparity between the flat-compressed-cached curve and any HNSW solution isn’t notably significant. Some may argue that ~100,000 vectors isn’t a substantial amount, and that’s a valid point. However, let’s discuss the implications of combining this feature with Multi-tenancy.</p> <p>Weaviate ensures total information isolation for each tenant. Imagine having ~1000 tenants, each with ~100,000 vectors. Surprisingly, the expected performance remains more or less consistent. Suddenly, 100 million vectors constitute a substantial amount of data. Moreover, Weaviate facilitates rapid tenant deactivation/lazy reactivation, enabling the creation of an exceptionally performant solution with an incredibly low memory footprint, provided you’ve designed a robust architecture.</p> <p>Now, let’s scale the numbers up further. What should we anticipate with a larger dataset? Unfortunately, brute force scales linearly with the data size. If we were to increase the dataset to 1,000,000 vectors, the QPS would be roughly 10 times slower than those exhibited here. However, even with this increased latency, brute force remains a viable option for certain applications.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image13.png’).default} alt=”latency 1000k” style= /&gt; &lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image20.png’).default} alt=”latency 1000k 2” style= /&gt;</p> <p><strong>Fig</strong>: <em>QPS vs Recall for 1000k DBPedia vectors comparing various indexes.</em></p> <h2 id="️comparing-product-and-binary-quantization">⚖️Comparing Product and Binary Quantization</h2> <p>Now that you have multiple quantization techniques available in Weaviate the question arises which one is better and where to use PQ vs. BQ. This decision will come down to your specific data and will require you to run your own benchmarks. We make code and instructions on how to do this available in the next section. The memory and performance experiments below are meant to make the PQ vs. BQ choice a little easier for you.</p> <p>&lt;img src={require(‘/assets/img/posts/2024-04-02-binary-quantization/image22.png’).default} alt=”latency PQ v BQ” style= /&gt;</p> <p><strong>Fig</strong>: <em>QPS vs Recall for 1000k DBPedia vectors comparing HNSW with and without PQ and BQ compression.</em></p> <p>Notice that the main contribution of BQ is not just that it compresses the vectors. The more efficient bitwise distance calculations also play a significant role. This is why the flat+bq option we discussed above is such a good choice. Not only do we need to read less from disk, but also, the faster distance calculations make brute force faster in Weaviate.</p> <p>| <strong>Index</strong> | <strong>Indexing Time</strong> | <strong>Memory Usage</strong> | |——————|—————|————–| | HNSW | 8m42s | 6437.05MB | | HNSW+PQ | 21m25s | 930.36MB | | HNSW+BQ | 3m43s | 711.38MB | | FLAT+BQ | 54s | 260.16MB | | FLAT+BQ+CACHE | 53s | 352.99MB | <strong>Table 4</strong>: <em>Comparing the various indexes and compression techniques.</em></p> <p>Notice how BQ shortens the indexing time with HNSW enormously.</p> <h2 id="benchmarking-bq-with-your-own-data">🧑‍💻Benchmarking BQ with your own data</h2> <p>Here we provide code and instructions that will help you to balance recall, latency, and memory footprint optimally by allowing you to replicate these above experiments yourself on your data.</p> <p>We have included some very useful tools in this <a href="https://github.com/weaviate/weaviate-chaos-engineering/tree/main/apps/ann-benchmarks">repository</a>. To easily run these tests (or any test using your data) you would need the data to be in <code class="language-plaintext highlighter-rouge">hdf5</code> format with the same specifications as described in the <a href="https://ann-benchmarks.com/">ANN Benchmarks</a>. You could index your data using the <a href="https://github.com/weaviate/weaviate-benchmarking">Go benchmarker</a>. This benchmarker could give you a better idea of QPS using concurrent queries. It takes several parameters that you could explore but in our runs, we use the following commands:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>go run main.go ann-benchmark <span class="nt">-v</span> ~/Documents/datasets/dbpedia-100k-openai-ada002.hdf5 <span class="nt">-d</span> cosine <span class="nt">--indexType</span> flat
</code></pre></div></div> <p>Notice the <code class="language-plaintext highlighter-rouge">-d</code> parameter for the distance and –indexType for switching between hnsw and flat.</p> <p>To run compressed (BQ enabled):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>go run main.go ann-benchmark <span class="nt">-v</span> ~/Documents/datasets/dbpedia-100k-openai-ada002.hdf5 <span class="nt">-d</span> cosine <span class="nt">--indexType</span> flat <span class="nt">--bq</span> enabled
</code></pre></div></div> <p>Notice the <code class="language-plaintext highlighter-rouge">-bq</code> parameter is used to activate compression.</p> <p>Once you run the script, you will see the different metrics in your terminal at the end of the run. Pay special attention to the QPS and recall. The results will be saved in JSON format under a repository named <code class="language-plaintext highlighter-rouge">results</code> in the same path as the script. Next, you can also run the <code class="language-plaintext highlighter-rouge">visualize.py</code> script to produce the same graphs we show in this post. Your graphs will be available in the same path as the script as <code class="language-plaintext highlighter-rouge">output.png</code>.</p> <p>Happy compressing!🚀</p> <p>import WhatNext from ‘/_includes/what-next.mdx’</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="research"/><category term="engineering"/><summary type="html"><![CDATA[In-depth technical breakdown of how binary quantization works and how to use it in Weaviate.]]></summary></entry><entry><title type="html">Accelerating Vector Search up to +40% with Intel’s latest Xeon CPU - Emerald Rapids</title><link href="https://zainhas.github.io/blog/2024/intel/" rel="alternate" type="text/html" title="Accelerating Vector Search up to +40% with Intel’s latest Xeon CPU - Emerald Rapids"/><published>2024-03-26T00:00:00+00:00</published><updated>2024-03-26T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2024/intel</id><content type="html" xml:base="https://zainhas.github.io/blog/2024/intel/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-03-26-intel/hero-480.webp 480w,/assets/img/posts/2024-03-26-intel/hero-800.webp 800w,/assets/img/posts/2024-03-26-intel/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2024-03-26-intel/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="'Accelerating Vector Search up to +40% with Intel’s latest Xeon CPU - Emerald Rapids'" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Overview of Key Sections:</strong></p> <ul> <li><a href="#vector-distance-calculations"><strong>Vector Distance Calculations</strong></a> Different vector distance metrics popularly used in Weaviate.</li> <li><a href="#vector-distance-implementations"><strong>Implementations of Distance Calculations in Weaviate</strong></a> Improvements under the hood for implementation of Dot product and L2 distance metrics.</li> <li><a href="#enter-intel-emerald-rapids"><strong>Intel’s 5th Gen Intel Xeon Processor, Emerald Rapids</strong></a> More on Intel’s new 5th Gen Xeon processor.</li> <li><a href="#lets-talk-numbers"><strong>Benchmarking Performance</strong></a> Performance numbers on microbenchmarks along with simulated real-world usage scenarios.</li> </ul> <p>What’s the most important calculation a vector database needs to do over and over again? What simple operation does it spend the majority of its time performing?</p> <p>If you guessed <strong>vector distance calculations</strong> … BINGO! 🎉</p> <p>While vector databases use many techniques and algorithms to improve performance (including locality graphs, quantization, hash based approaches), at the end of the day, efficient distance calculations between high-dimensional vectors is a requirement for a good vector database. In fact, when profiling Weaviate indexed using HNSW, we find that 40%-60% of the CPU time is spent doing vector distance calculations.</p> <p>So when someone tells us that they can make this quintessential process <em>much faster</em> they have our full attention! If you want to learn how to leverage algorithmic and hardware optimizations to make vector search 40% faster keep reading!</p> <p>In this post we’ll do a technical deep dive into different implementations for vector distance calculations and optimizations enabled by Intel’s new 5th Gen Xeon Processor - Emerald Rapids, parallelization techniques using SIMD with the Intel AVX-256 and Intel AVX-512 instruction sets, loop unrolling and compiler optimizations by transpiling C to Go assembly. We explain how we attained a <strong>~40% QPS speed up at 90% Recall</strong> in Weaviate running on Intel’s new Xeon Processor, Emerald Rapids.</p> <h2 id="vector-distance-calculations">Vector Distance Calculations</h2> <p>Vector databases use vector representations generated from machine learning models to capture the meaning of data. The fact that these vectors are numerical representations of data objects allows us to apply mathematical operations to them, such as calculating the distance between two vector representations to determine their similarity.</p> <p>To calculate the distance between two vectors, you can use several similarity measures, detailed below:</p> <p><img src="/assets/img/posts/2024-03-26-intel/vector_distance.png" alt="Vector Distance Calculations"/></p> <p>As you can see, there are many different similarity measures - cosine, dot product or euclidean (also know as L2) are the most commonly used in practice. To learn more about the different distance metrics, you can continue reading our blog post on <a href="/blog/distance-metrics-in-vector-search/">What are Distance Metrics in Vector Search?</a></p> <p>When a vector database needs to retrieve semantically similar objects it has to calculate one of these distance metrics to assess how close a query point is to candidate points in the database. In fact, this vector distance calculation needs to be conducted when you build the index as well as anytime you perform any CRUD operation.</p> <p>The runtime complexity for these vector distance operations scales linearly with the dimensionality of the vector. Typically vector dimensionality tends to be anywhere from 256 to 4096 in practice - this number is trending upwards over time - which means that distance calculations are also taking longer to perform.</p> <p>This being the case, we need to ensure that this operation is done as fast and efficiently as possible, whether through leveraging hardware or software efficiencies.</p> <p>Let’s take a look at how these calculations can be implemented, for simplicity’s sake we will consider the dot product only.</p> <h2 id="vector-distance-implementations">Vector Distance Implementations</h2> <h3 id="naive-go-implementation">Naive Go Implementation</h3> <p>The simplest way to implement the dot product in Go is to loop over the vector dimensions one at a time and sum the product of the vectors. This is quite slow for higher dimensionality but it works well for low dimensional vectors.</p> <pre><code class="language-Go">func dotProduct(a, b []float32) float32 {

    var result float32

    for i := 0; i &lt; len(a); i++ {

        result += a[i] * b[i]

    }
    return result
}
</code></pre> <h3 id="introduction-to-simd">Introduction to SIMD</h3> <p>SIMD, which stands for Single Instruction, Multiple Data, is a parallel processing technique that allows a single instruction to operate on multiple data elements simultaneously. This approach is commonly used to improve the performance of various algorithms, including those involving vector and matrix operations.</p> <p>In the context of vector distance calculations, SIMD instructions can be used to calculate distances between vectors more efficiently. By grouping the dimensions of the vectors into smaller chunks (e.g., 128-bit or 256-bit registers), SIMD instructions can perform the same operation on multiple dimensions in parallel, chunk by chunk. This parallel processing capability can lead to significant performance improvements when dealing with high-dimensional data.</p> <p><img src="/assets/img/posts/2024-03-26-intel/SIMD.png" alt="SIMD"/></p> <p>Intel CPUs feature support for advanced SIMD instruction sets, with notable examples being AVX-256 and AVX-512. AVX-256 enables the simultaneous processing of 8 32-bit floating-point numbers (8 dimensions), while AVX-512 doubles this capability to 16 dimensions.</p> <p>Weaviate uses Go assembly to generate SIMD instructions. In doing this we give up code readability for performance and so we reserve this technique for distance functions alone where every ounce of performance is needed.</p> <p>Rather than writing Go assembly directly, we write the code in C and then use the <a href="https://github.com/gorse-io/goat">Go goat library</a> which uses Clang/LLVM to generate the final Go assembly. This compiler optimization further improves performance.</p> <p>Below is an example implementation for dot product operation using SIMD AVX-512 intrinsics carried out for vector dimensions greater than 128:</p> <pre><code class="language-Go">if (n &gt;= 128) {

    __m512 acc = _mm512_setzero_ps();

    while (n &gt;= 128) {
      
        __m512 a_vec, b_vec;
        for (int i = 0; i &lt; 8; ++i) {

            a_vec = _mm512_loadu_ps(a + i * 16);
            b_vec = _mm512_loadu_ps(b + i * 16);

            acc = _mm512_fmadd_ps(a_vec, b_vec, acc);
        }
        n -= 16;
        a += 16;
        b += 16;
    }
}
</code></pre> <h3 id="loop-unrolling">Loop Unrolling</h3> <p>We further improve the above implementation by using a technique called loop unrolling. In a simple implementation where data dependencies exist between loop iterations, such as those involving variables like ‘i’ and ‘sum’, each iteration must wait for the previous one to complete. Loop unrolling addresses this issue by rewriting the loop to allow more high-latency instructions, like multiplication, to execute concurrently. Moreover, it spreads out the fixed loop costs, such as increment and comparison operations, across multiple operations, thereby enhancing overall CPU efficiency.</p> <h3 id="using-both-avx-256-and-avx-512">Using both AVX-256 and AVX-512</h3> <p>While AVX-256 and AVX-512 significantly enhance computational throughput, it’s important to note that there are trade-offs. Creating, loading, and reducing registers associated with these instruction sets come at a computational cost, making them less suitable for low-dimensional data processing tasks.</p> <p>A key difference in our current approach is a hybrid instruction usage strategy tailored to the dimensionality of the vectors being processed. Through our testing, we determined that the optimal performance of AVX-256 and AVX-512 is achieved when processing dimensions above 8 - which is why we fall back to the naive implementation mentioned above for these lower dimensionality use cases. When dealing with dimensions ranging from 8 to 128, we found that using AVX-256 instructions with loop unrolling yields the most favorable results.</p> <p>In the past, we conducted experiments with AVX-512 to assess its performance potential. However, the results didn’t show a significant increase in performance, particularly on older Intel processors. This lack of notable improvement could be due to thermal and power constraints in these older processor architectures. Such constraints could lead to <a href="https://lemire.me/blog/2018/09/07/avx-512-when-and-how-to-use-these-new-instructions/">reductions in CPU frequency</a>, limiting the capacity of AVX-512 instructions to deliver enhanced performance under these conditions. As a result, previously we only used AVX-256.</p> <p>This has significantly changed with the newer Sapphire and Emerald Rapids architectures where we have switched to using AVX-512 instructions to achieve further efficiencies for higher dimensional vectors.</p> <p>:::tip Bob van Luijt, Cofounder and CEO – Weaviate “Inference requires significant computing power, making it crucial to utilize the Intel next-generation Xeon platform for incredible performance gains. We are thrilled about our partnership with Intel and the opportunity to offer these advantages to our customers quickly through our cloud partnerships” :::</p> <h2 id="enter-intel-emerald-rapids">Enter Intel Emerald Rapids</h2> <p>Thanks to the Intel Disruptor Initiative, we recently had the opportunity to preview Intel’s latest innovation, the 5th Gen Xeon Scalable server processors based on the Intel 7 node - Emerald Rapids. This new architecture introduces a fifth turbo level, enabling workloads with high AVX-512 utilization to achieve higher frequencies. This provides large benefits to AVX-512 performance, delivering up to a remarkable 10x increase in performance when enabled.</p> <p>:::tip Arijit Bandyopadhyay, CTO – Enterprise Analytics and AI, Head of Strategy – Cloud &amp; Enterprise CSV Group at Intel “Weaviate’s collaboration with Intel Disruptor Program has been defining new ground for Generative AI in the industry. Weaviate’s product offerings on the latest Intel Technologies provides the best Gen AI solutions for enterprises, paving the way for greater ROI and faster Time to Market.” :::</p> <p>&lt;img src={require(‘/assets/img/posts/2024-03-26-intel/intel_slide.png’).default} alt=”Intel Emerald Rapids” style= /&gt;</p> <h2 id="lets-talk-numbers">Let’s Talk Numbers!</h2> <p>To benchmark the improvements we opted to conduct a performance evaluation of Weaviate using Intel’s Emerald Rapids 5th Gen Xeon Processor, simulating real-world usage scenarios. Initially, we devised a Go micro benchmark to compare the performance of the two instruction sets across L2 and dot product distance metrics. The results, depicted in the graphs below, provide valuable insights into the relative efficiencies of AVX-256 and AVX-512 instructions.</p> <figure> &lt;img src={require('/assets/img/posts/2024-03-26-intel/micro_bench.png').default} alt="Micro benchmarks" style= /&gt; <figcaption>Figure 1: Improvements in vector distance operations performed using 1536 dimensional vectors.</figcaption> </figure> <p>As you can see above we get some pretty remarkable speedups for single distance calculations. For dot product operations at 1536 dimensions, AVX-512 is 28.6 times faster than pure Go and 6 times faster than AVX-256. Whereas for L2 distance measurements at 1536 dimensions, AVX-512 is 19 times faster than pure Go and 5.6 times faster than AVX-256.</p> <p>Subsequently, we conducted ANN benchmarks utilizing two different <a href="http://dev.dbpedia.org/Download_DBpedia">datasets DBPedia</a> embedded using OpenAI <code class="language-plaintext highlighter-rouge">text-embedding-ada-002</code> model (1536 dimensions), and Meta’s 768 dimension <a href="/blog/sphere-dataset-in-weaviate">Sphere dataset</a>, aiming to gauge the practical implications of these instructions on real-world tasks. As seen in the graphs the benchmark results showed notable performance improvements, further validating the advantages of AVX-512 over AVX-256 in Emerald Rapids.</p> <figure> &lt;img src={require('/assets/img/posts/2024-03-26-intel/dbpedia_bench.png').default} alt="DBPedia benchmarks" style= /&gt; <figcaption>Figure 2: Intel Emerald Rapids 5th Gen Xeon ANN performance on DBPedia ada-002 embeddings.</figcaption> </figure> <figure> &lt;img src={require('/assets/img/posts/2024-03-26-intel/sphere_bench.png').default} alt="Sphere benchmarks" style= /&gt; <figcaption>Figure 3: Intel Emerald Rapids 5th Gen Xeon ANN performance on Sphere embeddings.</figcaption> </figure> <p>Here on the sphere-1 million dataset when searched using dot products we see a remarkable 42% improvement in Weaviate queries per second(QPS) @ 90% Recall, while on the dbpedia-500k dataset when searched using L2 distance we see a 40% improvement in QPS @ 90% Recall!</p> <p>Furthermore, our assessment revealed a 16% enhanced AVX-512 performance in Intel’s 4th Gen Xeon Processor, Sapphire Rapids compared to AVX-256. As a result, we made the strategic decision to enable the latest Intel Xeon Gen Processors, both Emerald Rapids and Sapphire Rapids, to leverage AVX-512 instructions starting from Weaviate version <code class="language-plaintext highlighter-rouge">1.24.2</code>. This proactive measure ensures that users can harness the full potential of their hardware, now and in the future.</p> <p>import WhatsNext from ‘/_includes/what-next.mdx’;</p> <WhatsNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="engineering"/><category term="research"/><summary type="html"><![CDATA[Boosting Weaviate using SIMD-AVX512, Loop Unrolling and Compiler Optimizations]]></summary></entry><entry><title type="html">Multimodal Retrieval-Augmented Generation (RAG)</title><link href="https://zainhas.github.io/blog/2023/multimodal-RAG/" rel="alternate" type="text/html" title="Multimodal Retrieval-Augmented Generation (RAG)"/><published>2023-12-05T00:00:00+00:00</published><updated>2023-12-05T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2023/multimodal-RAG</id><content type="html" xml:base="https://zainhas.github.io/blog/2023/multimodal-RAG/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2023-12-05-multimodal-RAG/hero-480.webp 480w,/assets/img/posts/2023-12-05-multimodal-RAG/hero-800.webp 800w,/assets/img/posts/2023-12-05-multimodal-RAG/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2023-12-05-multimodal-RAG/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Multimodal Retrieval-Augmented Generation (RAG)" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Multimodal Retrieval-Augmented Generation (MM-RAG) extends the capabilities of traditional <a href="/blog/introduction-to-rag">Retrieval-Augmented Generation (RAG)</a> systems from text-only modalities to other modalities, like images, audio, and video. The multimodal aspect refers to both the retrieval pipeline with <a href="/blog/multimodal-models">multimodal embedding models</a> the generation pipeline with multimodal generative models, such as vision language models.</p> <p><a href="https://youtu.be/Ckz8XA2hW84?t=2286">The average human hears and learns from about 1 billion words in their entire lifetime</a>. This might be an over-approximation, but it is in the correct ballpark because 1 billion seconds is about 30 years, and we don’t hear more than a few words per second. Accounting for sleeping, eating, and other activities, doing some back-of-the-napkin calculations, we can arrive at the above number.</p> <p>The issue, however, is that current Large Language Models (LLMs) are trained on trillions of tokens, many orders of magnitude more data than we ever see in our lifetime. Yet they still don’t have as vivid of an understanding of the causal relationships that exist in the world. From this, we can infer that the way humans learn is fundamentally different from how our current state-of-the-art models learn. Humans have a remarkable ability to learn and build world models through the integration of multiple sensory inputs. Our combination of senses works synergistically to provide us with rich and diverse information about our environment. By combining and interpreting these sensory inputs, we can form a coherent understanding of the world, make predictions, acquire new knowledge, and establish causal relationships very efficiently. Not only do humans capture and use multimodal representations of information, but given a task, we can also incorporate any of these modalities as context to help us guide our answers.</p> <p>If you’d like to explore this line of thinking further and the potential problems that need to be addressed when getting computers to take advantage of multimodal data, have a look at my previous <a href="/blog/multimodal-models">blog on multimodal embedding models</a>.</p> <p>:::info TLDR In this post, we’ll touch on:</p> <ul> <li><strong>Contrastive Learning:</strong> One particular approach to training multimodal embedding models that can understand images, audio, video, text, and more</li> <li><strong>Any-to-Any Search and Retrieval:</strong> Using multimodal embedding models to perform any-to-any search and scaling these multimodal embeddings into production using <a href="/blog/what-is-a-vector-database">vector databases</a>, like Weaviate (<em>with code examples!</em>)</li> <li><strong>Multimodal Retrieval-Augmented Generation (MM-RAG)</strong>: Augmenting the generation from Large Multimodal Models (LMMs) with multimodal information retrieval of images and more for visual question answering systems.</li> <li><strong>Code Demo of RAG</strong> :::</li> </ul> <h2 id="joint-embedding-space-through-contrastive-learning">Joint Embedding Space Through Contrastive Learning</h2> <p>One way to train a model that understands multimodal data including images, audio, video, and text is to first train individual models that understand each one of these modalities separately and then unify their representations of data using a process called contrastive training.</p> <p>Contrastive training unifies the vector space representation of models by pushing conceptually different embeddings from different modalities further apart or pulling similar ones closer together. This is demonstrated in the image below:</p> <p><img src="/assets/img/posts/2023-12-05-multimodal-RAG/image_a.png" alt="tripletLoss"/> <em>Source: <a href="https://arxiv.org/abs/1503.03832">Schroff et al. 2015</a></em></p> <p>This process was carried out in MetaAI’s <a href="https://arxiv.org/abs/2305.05665">ImageBind paper</a> to unify vector spaces across 6 different modalities, including images, text, audio, and video. To successfully perform contrastive training they used multiple labeled datasets of positive points across multiple modalities and randomly sampled for negative points.</p> <p>To get a better intuitive understanding of how this process works, imagine you embed the image of a lion into vector space using a vision model. The concept behind this object is similar to the audio of a lion roaring, so the audio object embedding can be used as a positive sample,e and the contrastive loss function works to pull these two points together in embedding space. On the other hand, the embedding of an image of a salad is a negative example and therefore needs to be pushed apart. Have a look at the modification of the above visual to account for cross-modal contrastive training:</p> <p><img src="/assets/img/posts/2023-12-05-multimodal-RAG/image_b.png" alt="crossMCLR"/> <em>Source: <a href="https://lmb.informatik.uni-freiburg.de/Publications/2021/ZB21/">Zolfaghari et al. 2021</a></em></p> <p>If we can continually do this for a large enough dataset of labeled points, then we can tighten the representations of data objects in embedding space and even unify the models of different modalities. Another benefit of ImageBind was the use of frozen image model representations to bind other modalities with cross-modal contrastive loss training. This is why it’s called ImageBind. Embeddings that start differently can then be pulled towards the image representations. Thus, all of the similar concepts across modalities can be unified such that a concept across all modalities will have similar vectors - demonstrated in the image below. To learn in more depth about contrastive representation learning, I would recommend this <a href="https://lilianweng.github.io/posts/2021-05-31-contrastive/">blog</a>.</p> <p><img src="/assets/img/posts/2023-12-05-multimodal-RAG/unified_emb_D.png#gh-dark-mode-only" alt="unified_emb_Dark"/> <img src="/assets/img/posts/2023-12-05-multimodal-RAG/unified_emb_L.png#gh-light-mode-only" alt="unified_emb__Light"/> <em>Shows a unified embedding model that captures meanings from any modality that was fused during the contrastive training step.</em></p> <h2 id="any-to-any-search">Any-to-Any Search</h2> <p>Once we have a unified embedding space, we can perform cross-modal object operations such as cross-modal search and retrieval. This means that we can pass in as a query any modality the model understands and use it to perform <a href="/blog/vector-search-explained">vector similarity search</a> in multimodal embedding space, getting back objects of any other modality that are similar in concept. You can also use this unified embedding space to perform cross-modal embedding arithmetic. For example, you can answer questions like what an image of a pigeon and the audio of a bike revving look like together. <img src="/assets/img/posts/2023-12-05-multimodal-RAG/cross_modal.png" alt="cross_modal"/> <em><a href="https://imagebind.metademolab.com/">Source</a></em></p> <p>In <a href="https://github.com/weaviate-tutorials/multimodal-workshop/blob/main/2-multimodal/1-multimedia-search-complete.ipynb">this Jupyter notebook</a>, we show how you can use the <code class="language-plaintext highlighter-rouge">multi2vec-bind</code> module in Weaviate to use the ImageBind model to add multimedia files to a vector database. Then we can perform any-to-any search over that data.</p> <p>You can also use this diagram explaining any-to-any search to get an intuition of how the following code leverages the unified embedding space previously generated to perform the search.</p> <p><img src="/assets/img/posts/2023-12-05-multimodal-RAG/any2any_D.png#gh-dark-mode-only" alt="any2any_Dark"/> <img src="/assets/img/posts/2023-12-05-multimodal-RAG/any2any_L.png#gh-light-mode-only" alt="any2any_Light"/> <em>Any-to-any search: Shows that any of the modalities understood and embedded by the multimodal model can be passed in as a query, and objects of any modality that are conceptually similar can be returned.</em></p> <h3 id="step-1-create-a-multimodal-collection">Step 1: Create a Multimodal Collection</h3> <p>First, you need to create a collection that can hold data in different modalities, such as audio, images, and video.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">client</span><span class="p">.</span><span class="n">collections</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Animals</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">vectorizer_config</span><span class="o">=</span><span class="n">wvc</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">Configure</span><span class="p">.</span><span class="n">Vectorizer</span><span class="p">.</span><span class="nf">multi2vec_bind</span><span class="p">(</span>
        <span class="n">audio_fields</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">audio</span><span class="sh">"</span><span class="p">],</span>
        <span class="n">image_fields</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">image</span><span class="sh">"</span><span class="p">],</span>
        <span class="n">video_fields</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">video</span><span class="sh">"</span><span class="p">],</span>
    <span class="p">)</span>
<span class="p">)</span>
</code></pre></div></div> <h3 id="step-2-insert-images-and-other-media">Step 2: Insert Images and Other Media</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">source</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">listdir</span><span class="p">(</span><span class="sh">"</span><span class="s">./source/image/</span><span class="sh">"</span><span class="p">)</span>
<span class="n">items</span> <span class="o">=</span> <span class="nf">list</span><span class="p">()</span>
<span class="k">for</span> <span class="n">name</span> <span class="ow">in</span> <span class="n">source</span><span class="p">:</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Adding </span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">path</span> <span class="o">=</span> <span class="sh">"</span><span class="s">./source/image/</span><span class="sh">"</span> <span class="o">+</span> <span class="n">name</span>
    <span class="n">items</span><span class="p">.</span><span class="nf">append</span><span class="p">({</span>
        <span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">:</span> <span class="n">name</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">path</span><span class="sh">"</span><span class="p">:</span> <span class="n">path</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">image</span><span class="sh">"</span><span class="p">:</span> <span class="nf">toBase64</span><span class="p">(</span><span class="n">path</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">mediaType</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">image</span><span class="sh">"</span>
    <span class="p">})</span>
<span class="n">animals</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">collections</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">Animals</span><span class="sh">"</span><span class="p">)</span>
<span class="n">animals</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">insert_many</span><span class="p">(</span><span class="n">items</span><span class="p">)</span>
</code></pre></div></div> <h3 id="step-3-performing-image-search">Step 3: Performing Image Search</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">response</span> <span class="o">=</span> <span class="n">animals</span><span class="p">.</span><span class="n">query</span><span class="p">.</span><span class="nf">near_image</span><span class="p">(</span>
    <span class="n">near_image</span><span class="o">=</span><span class="nf">toBase64</span><span class="p">(</span><span class="sh">"</span><span class="s">./test/test-cat.jpg</span><span class="sh">"</span><span class="p">),</span>
    <span class="n">return_properties</span><span class="o">=</span><span class="p">[</span><span class="sh">'</span><span class="s">name</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">path</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">mediaType</span><span class="sh">'</span><span class="p">],</span>
    <span class="n">limit</span><span class="o">=</span><span class="mi">3</span>
<span class="p">)</span>
</code></pre></div></div> <p>For a more detailed breakdown, refer to the <a href="https://github.com/weaviate-tutorials/multimodal-workshop/blob/main/2-multimodal/1-multimedia-search-complete.ipynb">complete notebook</a> and <a href="https://github.com/weaviate-tutorials/">supporting repository</a>.</p> <p>Using a vector database, like Weaviate, to store and perform fast and real-time retrieval of object embeddings allows us to scale the usage of these multimodal models. This allows us to power multimodal search in production and to integrate into our applications the cross-modal operations that we’ve discussed.</p> <h2 id="multimodal-retrieval-augmented-generation-mm-rag">Multimodal Retrieval-Augmented Generation (MM-RAG)</h2> <p>RAG allows us to pack retrieved documents into a prompt so that a language model can read relevant information before generating a response. This allows us to scale the knowledge of large language models without having to train or fine-tune them every time we have updated information.</p> <p>By externalizing the knowledge of a model, <a href="https://cs.stanford.edu/~myasu/blog/racm3/">RAG can provide</a> benefits such as:</p> <ul> <li><strong>Scalability</strong>: reducing the model size and training cost, as well as allowing easy expansion of knowledge</li> <li><strong>Accuracy</strong>: grounding the model to facts and reducing hallucinations</li> <li><strong>Controllability</strong>: allowing updating or customizing the knowledge by simply performing CRUD operations in a vector DB</li> <li><strong>Interpretability</strong>: retrieved documents serving as the reference to the source in model predictions</li> </ul> <p>However, the issue with text-only RAG systems is that they only leverage retrieved text source material. This is because most LLMs only understand language, and so any information that’s retrieved had to be in text format … until now!</p> <p>Recently, a group of large generative models, both closed and open source, has emerged that understand both text and images. As a result, we can now support multimodal RAG, where we can retrieve images from our vector database and pass those into the large multimodal model (LMM) to generate with. This simple two-step process, illustrated below, is the main idea behind multimodal RAG.</p> <p><img src="/assets/img/posts/2023-12-05-multimodal-RAG/mmRAG_D.png#gh-dark-mode-only" alt="mmRAG_Dark"/> <img src="/assets/img/posts/2023-12-05-multimodal-RAG/mmRAG_D.png#gh-light-mode-only" alt="mmRAG_Light"/> <em>Shows the two-step process of MM-RAG, involving retrieval from a multimodal knowledge base and then generation using a large multimodal model by grounding in the retrieved context.</em></p> <p>MM-RAG was <a href="https://arxiv.org/abs/2211.12561">presented earlier this year</a> by a group at Stanford. They showed a workflow of multimodal models, one that could retrieve and another that could generate both text and images.</p> <p>They also discussed the advantages of multimodal RAG:</p> <ul> <li>It significantly outperforms baseline multimodal models such as DALL-E and CM3 on both image and caption generation tasks.</li> <li>It requires much less compute while achieving better performance (&lt;30% of DALLE)</li> <li>MM-RAG capable models also generate images much more faithful to the retrieved context. This means the quality of the generated images is better and grounded in the retrieved context image.</li> <li>Multimodal models are capable of multimodal in-context learning (e.g., image generation from demonstrations). This means that we can feed any demonstration images and text so that the model generates an image that follows the visual characteristics of these in-context examples.</li> </ul> <p>MM-RAG gives us a way to further control the awesome generative power of these new multimodal models to produce more useful results for use in industry.</p> <h2 id="implementing-a-multimodal-rag-example-in-python">Implementing a Multimodal RAG Example in Python</h2> <p>This section demonstrates how you can implement a multimodal RAG system with a multimodal information retrieval system by retrieving from a multimodal collection and then stuffing a <code class="language-plaintext highlighter-rouge">base64</code> encoded image along with a text prompt to OpenAI’s GPT4 vision model. We then take the generated description and pass it into DALL-E-3 to recreate the image from the description.</p> <p>You can find the full code for this example in <a href="https://github.com/weaviate-tutorials/multimodal-workshop/blob/main/2-multimodal/2-multimedia-rag.ipynb">this Jupyter notebook</a>.</p> <h3 id="retrieve-an-object-from-a-multimodal-collection">Retrieve an Object from a Multimodal Collection</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">retrieve_image</span><span class="p">(</span><span class="n">query</span><span class="p">):</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">animals</span><span class="p">.</span><span class="n">query</span><span class="p">.</span><span class="nf">near_text</span><span class="p">(</span>
        <span class="n">query</span><span class="o">=</span><span class="n">query</span><span class="p">,</span>
        <span class="n">filters</span><span class="o">=</span><span class="n">wvc</span><span class="p">.</span><span class="n">query</span><span class="p">.</span><span class="nc">Filter</span><span class="p">(</span><span class="n">path</span><span class="o">=</span><span class="sh">"</span><span class="s">mediaType</span><span class="sh">"</span><span class="p">).</span><span class="nf">equal</span><span class="p">(</span><span class="sh">"</span><span class="s">image</span><span class="sh">"</span><span class="p">),</span>
        <span class="n">return_properties</span><span class="o">=</span><span class="p">[</span><span class="sh">'</span><span class="s">name</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">path</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">mediaType</span><span class="sh">'</span><span class="p">,</span><span class="sh">'</span><span class="s">image</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">limit</span> <span class="o">=</span> <span class="mi">1</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">objects</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">properties</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Retrieved image object:</span><span class="sh">"</span><span class="p">,</span><span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">(</span><span class="n">result</span><span class="p">,</span> <span class="n">indent</span><span class="o">=</span><span class="mi">2</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">result</span>
<span class="n">response</span> <span class="o">=</span> <span class="nf">retrieve_image</span><span class="p">(</span><span class="sh">"</span><span class="s">dog with a sign</span><span class="sh">"</span><span class="p">)</span>
<span class="n">SOURCE_IMAGE</span> <span class="o">=</span> <span class="n">response</span><span class="p">[</span><span class="sh">'</span><span class="s">image</span><span class="sh">'</span><span class="p">]</span>
</code></pre></div></div> <p><strong>Retrieved Image:</strong></p> <p><img src="/assets/img/posts/2023-12-05-multimodal-RAG/puppy.png" alt="puppy"/></p> <h3 id="generate-a-text-description-of-the-image">Generate a Text Description of the Image</h3> <p>Next, we will generate a text description of the image by using OpenAI’s GPT4-V vision language model.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">requests</span>

<span class="k">def</span> <span class="nf">generate_description_from_image_gpt4</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">image64</span><span class="p">):</span>
  <span class="n">headers</span> <span class="o">=</span> <span class="p">{</span>
      <span class="sh">"</span><span class="s">Content-Type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">application/json</span><span class="sh">"</span><span class="p">,</span>
      <span class="sh">"</span><span class="s">Authorization</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Bearer </span><span class="si">{</span><span class="n">openai</span><span class="p">.</span><span class="n">api_key</span><span class="si">}</span><span class="sh">"</span>
  <span class="p">}</span>
  <span class="n">payload</span> <span class="o">=</span> <span class="p">{</span>
      <span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt-4-vision-preview</span><span class="sh">"</span><span class="p">,</span>
      <span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
        <span class="p">{</span>
          <span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>
          <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
            <span class="p">{</span>
              <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span>
              <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="n">prompt</span>
            <span class="p">},</span>
            <span class="p">{</span>
              <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">image_url</span><span class="sh">"</span><span class="p">,</span>
              <span class="sh">"</span><span class="s">image_url</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
                <span class="sh">"</span><span class="s">url</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">data:image/jpeg;base64,</span><span class="si">{</span><span class="n">image64</span><span class="si">}</span><span class="sh">"</span> <span class="c1">#base64 encoded image from Weaviate
</span>              <span class="p">}</span>
            <span class="p">}</span>
          <span class="p">]</span>
        <span class="p">}</span>
      <span class="p">],</span>
      <span class="sh">"</span><span class="s">max_tokens</span><span class="sh">"</span><span class="p">:</span> <span class="mi">300</span>
  <span class="p">}</span>
  <span class="n">response_oai</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="nf">post</span><span class="p">(</span><span class="sh">"</span><span class="s">https://api.openai.com/v1/chat/completions</span><span class="sh">"</span><span class="p">,</span> <span class="n">headers</span><span class="o">=</span><span class="n">headers</span><span class="p">,</span> <span class="n">json</span><span class="o">=</span><span class="n">payload</span><span class="p">)</span>
  <span class="n">result</span> <span class="o">=</span> <span class="n">response_oai</span><span class="p">.</span><span class="nf">json</span><span class="p">()[</span><span class="sh">'</span><span class="s">choices</span><span class="sh">'</span><span class="p">][</span><span class="mi">0</span><span class="p">][</span><span class="sh">'</span><span class="s">message</span><span class="sh">'</span><span class="p">][</span><span class="sh">'</span><span class="s">content</span><span class="sh">'</span><span class="p">]</span>
  <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Generated description: </span><span class="si">{</span><span class="n">result</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
  <span class="k">return</span> <span class="n">result</span>
<span class="n">GENERATED_DESCRIPTION</span> <span class="o">=</span> <span class="nf">generate_description_from_image_gpt4</span><span class="p">(</span>
    <span class="n">prompt</span><span class="o">=</span><span class="sh">"</span><span class="s">This is an image of my pet, please give me a cute and vivid description.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">image64</span><span class="o">=</span><span class="n">SOURCE_IMAGE</span>
<span class="p">)</span>
</code></pre></div></div> <p><strong>Generated description:</strong> This adorable image captures a charming French Bulldog sitting obediently against a vibrant red background. The pup’s coat is predominantly white with distinctive black patches around the ears and eyes, giving it a look of natural elegance. Its expressive, wide-set eyes gleam with a mix of curiosity and anticipation, while the slight tilt of its head and those perky bat-like ears contribute to an overall image of endearing attentiveness.</p> <p>The cuteness is amplified by a handwritten sign hung around its neck with the words “FREE KISSES” and a little heart symbol, extending a sweet and whimsical offer to all who come near. The sign, coupled with the dog’s innocent gaze, conjures up feelings of warmth and companionship. This tiny ambassador of affection sits proudly, almost as if understanding the joy it brings to those around it. With its compact size and affectionate demeanor, this little canine looks ready to dispense unlimited love and puppy kisses on demand.</p> <h3 id="use-text-to-reconstruct-the-image-from-dall-e-3-diffusion-model">Use Text to Reconstruct the Image from DALL-E-3 (Diffusion Model):</h3> <p>Currently, GPT4-V can’t generate images. Therefore, we will use OpenAI’s DALL-E-3 model instead:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>
<span class="k">def</span> <span class="nf">generate_image_dalee3</span><span class="p">(</span><span class="n">prompt</span><span class="p">):</span>
  <span class="n">openai_client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">()</span>
  <span class="n">response_oai</span> <span class="o">=</span> <span class="n">openai_client</span><span class="p">.</span><span class="n">images</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">dall-e-3</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">prompt</span><span class="o">=</span><span class="nf">str</span><span class="p">(</span><span class="n">prompt</span><span class="p">),</span>
    <span class="n">size</span><span class="o">=</span><span class="sh">"</span><span class="s">1024x1024</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">quality</span><span class="o">=</span><span class="sh">"</span><span class="s">standard</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">n</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
  <span class="p">)</span>
  <span class="n">result</span> <span class="o">=</span> <span class="n">response_oai</span><span class="p">.</span><span class="n">data</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">url</span>
  <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Generated image url: </span><span class="si">{</span><span class="n">result</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
  <span class="k">return</span> <span class="n">result</span>
<span class="n">image_url</span> <span class="o">=</span> <span class="nf">generate_image_dalee3</span><span class="p">(</span><span class="n">GENERATED_DESCRIPTION</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Generated Image:</strong> <img src="/assets/img/posts/2023-12-05-multimodal-RAG/puppy_dalle.png" alt="generated puppy"/></p> <h2 id="recent-advancements-in-multimodal-rag">Recent Advancements in Multimodal RAG</h2> <p>Recent advancements in the retrieval mechanism with multimodal <a href="/blog/late-interaction-overview">late-interaction models</a>, such as ColPali, ColQwen, or ColQwen-Omni, as accelerated developments in multimodal RAG systems. In these modern AI systems, PDF documents are treated as multimodal documents that contain texts, images, tables, charts, etc.</p> <p>For an example implementation, refer to <a href="https://github.com/weaviate/recipes/blob/main/weaviate-features/multi-vector/multi-vector-colipali-rag.ipynb">this notebook showcasing multimodal RAG on PDF documents using ColPali</a></p> <h2 id="conclusion">Conclusion</h2> <p>In this blog, we covered how we can extend the concept of RAG to include retrieval from a multimodal knowledge base. We also explained how multimedia can be embedded into a unified vector space and consequently how we can leverage vector databases to power any-to-any search. I hope you found this article useful! I’d love to connect on <strong>X</strong> at <a href="https://twitter.com/ZainHasan6">@zainhasan6</a>!</p> <p>import WhatNext from ‘/_includes/what-next.mdx’</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="concepts"/><category term="how-to"/><summary type="html"><![CDATA[Learn how to build Multimodal Retrieval Augmented Generation (MM-RAG) systems that combine text, images, audio, and video. Discover contrastive learning, any-to-any search with vector databases, and practical code examples using Weaviate and OpenAI GPT-4V.]]></summary></entry><entry><title type="html">How to Reduce Memory Requirements by up to 90%+ using Product Quantization</title><link href="https://zainhas.github.io/blog/2023/pq-rescoring/" rel="alternate" type="text/html" title="How to Reduce Memory Requirements by up to 90%+ using Product Quantization"/><published>2023-09-19T00:00:00+00:00</published><updated>2023-09-19T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2023/pq-rescoring</id><content type="html" xml:base="https://zainhas.github.io/blog/2023/pq-rescoring/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2023-09-19-pq-rescoring/hero-480.webp 480w,/assets/img/posts/2023-09-19-pq-rescoring/hero-800.webp 800w,/assets/img/posts/2023-09-19-pq-rescoring/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2023-09-19-pq-rescoring/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="How to Reduce Memory Requirements by up to 90%+ using Product Quantization" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>:::info Highlights</p> <ul> <li><a href="#product-quantizationpq">Product Quantization</a> - how PQ reduces the memory requirements of running Weaviate</li> <li><a href="#the-problem-with-pq">Improving PQ with Rescoring</a> - why compressing vectors decreases recall and how the rescoring trick can help reduce this</li> <li><a href="#experiment-results">Experimental results</a> - QPS vs. recall, indexing times, and memory savings</li> <li><a href="#benchmarking-pq-with-your-own-data">Benchmarking PQ</a> - scripts for benchmarking PQ with your own data :::</li> </ul> <hr/> <h2 id="what-is-product-quantizationpq">📚What is Product Quantization(PQ)?</h2> <p>Product Quantization is a way to compress vectors, allowing users to save on memory requirements. To understand how product quantization (PQ) compression works, imagine every vector you want to store is a unique house address. This address allows you to precisely locate where someone lives including country, state, city, street number, and even down to the house number. The price you pay for this pin-point accuracy is that each address takes up more memory to store. Now, imagine instead of storing a unique address for each house we store just the city the house is located in. With this new representation, you can no longer precisely differentiate between houses that are all in the same city. The advantage however is that you require less memory to store the data in this new format. This is why PQ is a lossy algorithm; we lose information as a result of the compression - or in other words, you trade accuracy/recall for memory savings.</p> <p>Want to further reduce memory usage at the price of recall? Zoom out further and represent each address instead with the state it’s located in. Require more recall at the price of increased memory? Zoom in and represent the address on a more accurate scale. This tunable balancing of memory usage vs. recall is what makes PQ a great way to improve the flexibility of Weaviate for different use cases and customers.</p> <p><img src="/assets/img/posts/2023-09-19-pq-rescoring/Weaviate-release-1-18-vector-indexing-map.png#gh-light-mode-only" alt="pq compression"/> <img src="/assets/img/posts/2023-09-19-pq-rescoring/Weaviate-release-1-18-vector-indexing-map_1.png#gh-dark-mode-only" alt="pq compression"/></p> <p>Similar to the analogy above, in PQ compression instead of storing the exact vector coordinates, we replace them in memory with a learned code that represents the general region in which the vector can be found. Now imagine you conduct this compression for millions or even billions of vectors and the memory savings can become quite significant. Using these PQ compressed vectors stored in RAM we now start our search from our root graph quickly conducting broad searches over PQ compressed vector representations to filter out the best nearest neighbor candidates in memory and then drill down by exploring these best candidates when needed.</p> <p>If you’d like to dig deeper into the inner workings of PQ and how these compressed vector representations are calculated please refer to our previous blog <a href="https://weaviate.io/blog/ann-algorithms-hnsw-pq">here</a>.</p> <p>In this blog we cover how we can take this one step further, with 1.21 we introduced significant improvements to make PQ even more viable by reducing the recall price you pay in order to save on memory requirements. If you want to learn about these improvements and why using PQ should be a no-brainer now for most users, keep reading!</p> <p>:::info Enabling PQ To learn how to enable and configure Weaviate to use PQ along with HNSW refer to the docs <a href="https://docs.weaviate.io/weaviate/config-refs/schema/vector-index#how-to-configure-hnsw">here</a>. You can read more about HNSW+PQ in the documentation <a href="https://docs.weaviate.io/weaviate/concepts/vector-index#hnsw-with-product-quantizationpq">here</a>. :::</p> <h2 id="the-problem-with-pq">The problem with PQ</h2> <p>Our goal with the use of PQ is to reduce memory usage while maintaining a high quality of search - preferably the same search quality as when using uncompressed vectors. With the pure PQ implementation we had in v1.18 we paid a high recall/search quality price when the data was compressed at higher ratios.</p> <p>To demonstrate this loss of recall due to compression we show a compressed vs. uncompressed graph below. These results compare the HNSW+PQ compressed implementation versus using HNSW with uncompressed vectors.</p> <p>For our tests below, we use two different datasets: <a href="https://github.com/facebookresearch/sphere">Sphere</a> and <a href="https://www.dbpedia.org/about/">DBPedia</a>. The original Sphere dataset contains nearly 1 billion vectors of 768 dimensionality, however, in these initial experiments, we use a subset containing only 1 million vectors. We will revisit the entire dataset at the end of this post. DBPedia is a set of 1 million objects vectorized using the <code class="language-plaintext highlighter-rouge">Ada002</code> vectorizer from OpenAI. These are both sizable and high-dimensional realistic datasets that could very well be used for real-world applications.</p> <p><img src="/assets/img/posts/2023-09-19-pq-rescoring/im2.png" alt="graph1"/> <strong>Fig. 1</strong>: <em>The figure above shows results on a 1M subset of Sphere. The orange curve represents the results obtained with the uncompressed version. The blue curve shows the results of compressing using 6 dimensions per segment. This would achieve a 24:1 compression rate. Notice how the performance is comparable but the recall drops drastically.</em></p> <p><img src="/assets/img/posts/2023-09-19-pq-rescoring/im3.png" alt="graph2"/> <strong>Fig. 2</strong>: <em>The figure above shows results on the DBPedia dataset. The orange curve represents the results obtained with the uncompressed version. The blue curve shows the results of compressing using 6 dimensions per segment - compressing by a factor of 24x. Again, performance is preserved but the recall drops drastically.</em></p> <p>A quick explanation for the dip in the graph at the left end. Those curves were obtained by querying the server right after indexing the data. After the indexing, it normally follows intense operations such as compactions or garbage collection cycles. After those operations finish, the full CPUs are again ready and there are no locks on the data. Notice how normally the second point in the curve is always smooth again.</p> <p>Coming back to the main thread, this graph paints a pretty bleak picture! It seems inevitable that the more you compress the vectors, the more you will distort them and the further recall will drop. So how do we reduce this drop in recall while still achieving high compression ratios?🤔It starts with understanding: Why did the recall drop so drastically in the first place?</p> <h2 id="improving-recall-with-product-quantizationpq">Improving recall with product quantization(PQ)</h2> <p>The reason for this reduction in recall, across both experiments, is that after compression you only have 1/24th the amount of information stored in the vectors - the vectors are only a fuzzy estimation of what they used to be. Originally, we had a continuous vector space. After compression, we only have a discrete/quantized vector space. This is because all the vectors are now encoded with approximate groups of regions rather than unique coordinates - refer to the diagram below for a better understanding of what we mean by “regions”.</p> <p><img src="/assets/img/posts/2023-09-19-pq-rescoring/xyzPQ.jpg" alt="pqSpace"/> <strong>Fig. 3</strong>: <em>Here all the vectors that lie inside a region will have the same code - the coordinates of the white dot in that region - this is shown using a dotted line from the vector to the centroid. This is what allows us to save space, but also reduces recall since the vectors within the regions become indistinguishable from each other.</em></p> <table> <tbody> <tr> <td>If we have three vectors $x$, $y$ and $z$ represented with the closest centroids to them shown in white above, and if we measure the distance between $x$ and $z$ compared to $x$ and $y$ and if these are equal, $</td> <td>x - z</td> <td>=</td> <td>x - y</td> <td>$, then all we know about $z$ and $y$ is that they used to be close to each other prior to compression - we can’t calculate how close they were, additionally we can no longer calculate if $y$ is closer to $x$ or $z$ since we don’t have exact distances to compare. As a result, when the nearest neighbors algorithm calculates the distances between the query and compressed vectors it might not always identify the most relevant vectors to return to the user.</td> </tr> </tbody> </table> <p>Imagine you were invited to a birthday party in another country and instead of being given the full address you were only told the city where the party is! You might be able to roughly search up and travel to the right city in the country but finding the party within the city would be impossible. Try as hard as you might, you’re not going to be showing up to that party! 😅</p> <p>Similarly, we can use our compressed vectors to perform a rough search to identify objects that might be relevant or not and land us in the right “city” in vector space, but we still need a way to perform a finer search as we get closer to our target query. This finer search is made possible by the fact that we actually store the full un-compressed representation of the vectors on disk and can read them into working memory when/if required! Aha! A solution presents itself!</p> <p>Putting all the pieces together let’s say you get a query from the user, this gets converted into a vector and we start to perform a rough search comparing the query vector to compressed object vectors, to identify which objects are relevant (close enough) - if an object is found to be relevant we put it in the relevant bucket otherwise we throw it out. Once we have our bucket of relevant vectors we then fetch their uncompressed representations from disk into memory and use this to rescore the vectors in our bucket from closest to furthest to our query vector!</p> <p>Thus, this rough search followed by a finer search and rescoring allows us to reduce memory usage while still maintaining recall. Seems perfect no, why wouldn’t we always do this!? We get to have our cake and eat it too! Not quite!</p> <p>The only way recall would be lost when employing this new strategy is if the rough search over compressed vectors fails to identify some relevant vectors and these candidates never make it into the bucket for the finer rescoring search phase. Let’s consider some implementation details that can help avoid this potential failure mode.</p> <h2 id="the-devil-is-in-the-details">The devil is in the details</h2> <p>Before digging deeper, let’s think about the final goal we’re trying to solve.</p> <p>We use PQ because we want to save on memory. If we only have a few hundred thousand vectors we wouldn’t consider using PQ - as memory usage would be negligible. On the other hand, if we need to store billions of vectors then PQ becomes critically important - without it, we might not even be able to store all of our data. The problem that arises, however, is that a billion object vector space is quite crowded and since vectors are represented using regions/clusters with PQ encoding you’re likely to find more vectors in any given region - each of which will have the same coded representation. Refer to the diagram below for an intuitive representation of this “crowding” effect.</p> <p>|100K Vector Space | 1B Vector Space | |:————————-:|:————————-: | | <img src="/assets/img/posts/2023-09-19-pq-rescoring/100kVec_space.png" alt="100kVec_space"/> | <img src="/assets/img/posts/2023-09-19-pq-rescoring/1BVec_space.png" alt="1BVec_space"/> | <strong>Fig. 4</strong>: <em>Notice how the vector space representation on the bottom is a lot more crowded, due to there being more vectors(represented as black dots). Each of the vectors in a region will have the coordinates of the centroid for the region(represented as white dots). This means that all vectors in a region after compression become indistinguishable. Note that in PQ this is not exactly what happens but this does give us a good intuition of how the distortion is more noticeable when we have more vectors</em></p> <p>So the conundrum is this: the more data you have the more you will want to use PQ to compress it, but on the other hand, you’ll have more crowding which will distort data more and potentially reduce recall.</p> <p>To address this we explored two implementation strategies for our rescoring solution explained in the section above:</p> <ul> <li> <p><strong>Strategy 1:</strong> Conducting the rough search and finer search together. This is done by rescoring the distances during the search process.</p> </li> <li> <p><strong>Strategy 2:</strong> Conducting the finer search after the rough search has concluded. This is done by only rescoring after we have obtained the final bucket of relevant objects.</p> </li> </ul> <p>Strategy 1 gave us better recall because we got to determine object relevance using exact distances prior to putting them in our bucket - but performance suffered since we’re doing more disk reads to accomplish this.</p> <p>Strategy 2 ensured better performance - since we would do one disk read at the conclusion of the rough search, but recall suffered due to relevant objects not being included in our bucket during the rough search phase.</p> <p>In the PQ implementation of Weaviate <code class="language-plaintext highlighter-rouge">v1.21</code>, we have decided to adopt Strategy 2. This was because we found that its low recall problem could be addressed by tuning search parameters - or in other words by making our bucket of relevant objects larger. Simply stated, if we fear losing relevant objects during the rough search phase, due to PQ distortion, then storing more of them will reduce this. In our experiments, we found that using this technique we could achieve the same recall as Strategy 1 at lower latencies! Voila 🎉, we have a winner!</p> <h2 id="experiment-results">🔬Experiment Results</h2> <p>Below we show experiments comparing the new in improved <code class="language-plaintext highlighter-rouge">v1.21</code> PQ. Our experiments focus on recall, latency, indexing time, and memory savings.</p> <h3 id="qps-vs-recall-experiments">QPS vs Recall Experiments:</h3> <p>First, we show the comparison of the results of our old implementation (without rescoring nor the last optimizations) with the last implementation (v1.21). Our first point of comparison is in terms of performance (measured in QPS) against quality (measured in recall)</p> <p><img src="/assets/img/posts/2023-09-19-pq-rescoring/im4.png" alt="graph3"/> <strong>Fig. 5</strong>: <em>The image above shows the results in the Sphere dataset. We have included the four curves now. The results labeled as <code class="language-plaintext highlighter-rouge">compressed/uncompressed-1-18</code> refer to the old implementation while the other two represent the new <code class="language-plaintext highlighter-rouge">1.21</code> results achieved using rescoring and better code optimization.</em></p> <p><img src="/assets/img/posts/2023-09-19-pq-rescoring/im5.png" alt="graph4"/> <strong>Fig. 6</strong>: <em>The image above shows the results in the DBPedia dataset. We have included the four curves now. The results labeled as <code class="language-plaintext highlighter-rouge">compressed/uncompressed-1-18</code> refer to the old implementation while the other two represent the new results achieved using rescoring and better code optimization.</em></p> <p>As you might notice, both datasets follow similar profiles. There is a huge difference also between the uncompressed cases. This is because with version <code class="language-plaintext highlighter-rouge">1.19</code> of Weaviate we have introduced gRPC support and querying is much faster overall. We have also included SIMD optimizations for the distance calculations for the uncompressed case. Unfortunately, those SIMD optimizations cannot be used for our current compressed version - but those are details for another time!</p> <p>That being said, notice how the recall is no longer lower for the compressed version. To stress this out, if we compare the orange curve (compressed using <code class="language-plaintext highlighter-rouge">v1.18</code>) to the green curve (uncompressed using <code class="language-plaintext highlighter-rouge">v1.18</code>) we can notice the shifting to the left in the recall axis. Notice also that this shift is not small. Now comparing the red curve (compressed using <code class="language-plaintext highlighter-rouge">v1.21</code>) to the blue curve (uncompressed using <code class="language-plaintext highlighter-rouge">v1.21</code>) the two curves are very close together in terms of QPS and recall.</p> <h3 id="indexing-time-results">Indexing Time Results</h3> <p>Previously we were also experiencing longer indexing times when using PQ. The main bottleneck was distance calculations between already compressed vectors in the implementation of our pruning heuristic. To help with this issue, we have introduced a special lookup table with distances between compressed segments. This acts similarly to the lookup table centered at each query, but unlike the former which is more volatile, it will reside in memory during the full indexing process.</p> <p>Due to these improvements we now see improved indexing times, please refer to the profiles below. The table below shows the indexing times measured for all cases. Notice how the <code class="language-plaintext highlighter-rouge">v1.21</code> needs approximately the same time to index the data for both, compressed and uncompressed cases. This was not the case in earlier <code class="language-plaintext highlighter-rouge">v1.18</code> and was only possible with the introduction of the global lookup table.</p> <p>| Indexing Time | Version | Compressed (hours) | Uncompressed (hours)| |—————-|———-|——————–|————-| | Sphere | <code class="language-plaintext highlighter-rouge">1.18</code> | 1:01:19 | 0:30:03 | | | <code class="language-plaintext highlighter-rouge">1.21</code> | 0:26:42 | 0:30:03 | | DBPedia | <code class="language-plaintext highlighter-rouge">1.18</code> | 1:25:00 | 0:51:49 | | | <code class="language-plaintext highlighter-rouge">1.21</code> | 0:51:34 | 0:52:44 | <strong>Tab. 1</strong>: <em>Indexing time improvements between <code class="language-plaintext highlighter-rouge">v1.18</code> and <code class="language-plaintext highlighter-rouge">v1.21</code>.</em></p> <h3 id="memory-savings">Memory savings</h3> <p>Last but not least, our goal with compression is to save memory. We have seen two implementations indexing the data at the same speed and serving queries at very similar performance/quality profiles. The main benefit here is the memory footprint. The table below shows the heap memory used for both cases. We keep it simple this time and only show our last version (<code class="language-plaintext highlighter-rouge">1.21</code>) since the memory footprint is similar to the older version <code class="language-plaintext highlighter-rouge">1.18</code>.</p> <p>| Memory footprint (MB) | Compressed (hours) | Uncompressed (hours)| |—————-|——————–|————-| | Sphere | 725 (15%) | 5014 | | DBPedia | 900 (14%) | 6333 | <strong>Tab. 2</strong>: <em>Compression ratios of <code class="language-plaintext highlighter-rouge">~6.6x</code>.</em></p> <p>Notice how using the compressed version we would barely need 15% of the memory we would need under uncompressed settings.</p> <p>To close the results up, we have also indexed the full Sphere dataset (nearly 1 billion vectors) with and without compression. The full dataset without compression needed nearly <strong>2.7 Terabytes</strong> of memory while the compressed version only required <strong>0.7 Terabytes</strong> which is again, a huge saving in terms of memory and costs.</p> <h2 id="benchmarking-pq-with-your-own-data">📈Benchmarking PQ with your own data</h2> <p>If you’re trying to finetune HNSW and PQ parameters on your own data to optimally balance recall, latency, and memory footprint you might want to replicate these experiments yourself. While doing so, please pay special attention to the segments parameter. We have obtained the best results using segments of length 6 in our experiments with both: Sphere and OpenAI embeddings.</p> <p>We have included some very useful tools in our <a href="https://github.com/weaviate/weaviate-chaos-engineering">chaos pipelines</a>. To easily run these tests (or any test using your own data) you would need the data to be in <code class="language-plaintext highlighter-rouge">hdf5</code> format with the same specifications as described in the <a href="https://ann-benchmarks.com">ANN Benchmarks</a>. You could index your data using the <code class="language-plaintext highlighter-rouge">run.py</code> script under the <a href="https://github.com/weaviate/weaviate-chaos-engineering/tree/main/apps/ann-benchmarks">ann-benchmarks</a> folder. It admits several parameters that you could explore but in our runs, we use the following commands:</p> <p>To run uncompressed (PQ disabled):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 run.py <span class="nt">-v</span> sphere-1M-meta-dpr.hdf5 <span class="nt">-d</span> dot <span class="nt">-m</span> 32 <span class="nt">-l</span> <span class="nv">run</span><span class="o">=</span>uncompressed
</code></pre></div></div> <p>Notice the <code class="language-plaintext highlighter-rouge">-d</code> parameter for the distance and <code class="language-plaintext highlighter-rouge">-m</code> for the maximumConnections.</p> <p>To run compressed (PQ enabled):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python3 run.py <span class="nt">-v</span> sphere-1M-meta-dpr.hdf5 <span class="nt">-d</span> dot <span class="nt">-m</span> 32 <span class="nt">--dim-to-segment-ratio</span> 6 <span class="nt">-c</span> <span class="nt">-l</span> <span class="nv">run</span><span class="o">=</span>compressed
</code></pre></div></div> <p>Notice the <code class="language-plaintext highlighter-rouge">-c</code> flag to set compression on and the <code class="language-plaintext highlighter-rouge">–dim-to-segment-ratio</code> to specify how long you want your segments to be. In this case, we are using 6 dimensions per segment.</p> <p>Once you run the script, you will see the different metrics in your terminal at the end of the run. Pay special attention to the <code class="language-plaintext highlighter-rouge">qps</code>, <code class="language-plaintext highlighter-rouge">recall</code> and <code class="language-plaintext highlighter-rouge">heap_mb</code> (heap size in MB). You will also have the results in JSON format under a repository named <code class="language-plaintext highlighter-rouge">results</code> in the same path as the script. Next, you can also run the <code class="language-plaintext highlighter-rouge">visualize.py</code> script to produce the same graphs we show in this post. Your graphs will be available in the same path as the script as <code class="language-plaintext highlighter-rouge">output.png</code>. To differentiate between your different runs, use a different value for the run parameter when using the <code class="language-plaintext highlighter-rouge">run.py</code> script. The label you use for the run will be used for the curve generated after running the <code class="language-plaintext highlighter-rouge">visualize.py</code> script.</p> <h2 id="conclusions">Conclusions</h2> <p>In this post, we explore the details behind how we improved the implementation of PQ in Weaviate to reduce the loss of recall by introducing a rescoring trick that reads in uncompressed vectors from disk and recalculates exact distances during the search process.</p> <p>As a result of rescoring when using PQ in Weaviate the trade-off between memory savings and recall is great. For this reason, most applications and users of Weaviate can now use PQ as the default. To further improve your results when using PQ we suggest you benchmark with your own data to balance compression ratios against reduction in recall. Happy compressing!🚀</p> <p>import WhatNext from ‘/_includes/what-next.mdx’</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="research"/><category term="engineering"/><summary type="html"><![CDATA[The details behind how you can compress vectors using PQ with little loss of recall!]]></summary></entry><entry><title type="html">A Gentle Introduction to Vector Databases</title><link href="https://zainhas.github.io/blog/2023/vector-database/" rel="alternate" type="text/html" title="A Gentle Introduction to Vector Databases"/><published>2023-08-01T00:00:00+00:00</published><updated>2023-08-01T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2023/vector-database</id><content type="html" xml:base="https://zainhas.github.io/blog/2023/vector-database/"><![CDATA[<p>import Tabs from ‘@theme/Tabs’; import TabItem from ‘@theme/TabItem’; import FilteredTextBlock from ‘@site/src/components/Documentation/FilteredTextBlock’; import PyCode from ‘!!raw-loader!/_includes/code/automated-testing.py’; import TSCode from ‘!!raw-loader!/_includes/code/automated-testing.ts’;</p> <p><img src="img/hero.png" alt="A Gentle Introduction to Vector Databases"/></p> <p>If you have just recently heard the term “vector database” for the first time, you are not alone. Although vector databases have been around for a few years, they’ve just recently drawn the wider developer community’s attention.</p> <p>The excitement around vector databases - sometimes referred to as AI databases - is closely related to the release of ChatGPT. Since late 2022, the public has started to understand the capabilities of state-of-the-art large language models (LLMs), while developers have realized that vector databases can enhance these models further.</p> <p>This article will explain some of their core concepts, such as <a href="/blog/vector-embeddings-explained">vector embeddings</a> and <a href="/blog/vector-search-explained">vector search</a>. Then, we will dive into the technical details of using distance metrics in vector search and how <a href="https://docs.weaviate.io/weaviate/concepts/vector-index">vector indexes</a> enable efficient retrieval. Once we have a fundamental understanding, we will discuss use cases and the current tool landscape.</p> <h2 id="what-is-a-vector-database">What is a Vector Database?</h2> <p>A vector database indexes, stores, and provides access to structured or unstructured data (e.g., text or images) alongside its vector embeddings, which are the data’s numerical representation. It allows users to find and retrieve similar objects quickly at scale in production.</p> <p><img src="img/definition.png" alt="Definition"/></p> <p>Because of its search capabilities, it is sometimes also called a vector search engine.</p> <h2 id="how-do-vector-databases-work">How Do Vector Databases Work?​</h2> <p>Vector databases retrieve data objects with vector search. Vector search uses vector embeddings, which are machine-understandable formats of human-understandable data objects, such as text documents, images, songs, videos, and so on. Vector databases also use vector indexing to retrieve data objects at scale.</p> <p>This section introduces these core concepts. We will discuss what vector embeddings are, how they enable similarity search, and how they accelerate it.</p> <h3 id="vector-embeddings">Vector Embeddings​</h3> <p>When you think of data, you probably think of neatly organized numbers in spreadsheets. This type of data is called structured data because it can easily be stored in tabular format. However, roughly 80% of today’s data is said to be unstructured. Examples of unstructured data are images, text (e.g., documents, social media posts, or emails), or time series data (e.g., audio files, sensor data, or video). The problem with this type of data is that it is difficult to store it in an organized way so you can easily find what you are looking for.</p> <p>But innovations in Artificial Intelligence (AI) and Machine Learning (ML) have enabled us to numerically represent unstructured data without losing its semantic meaning in so-called vector embeddings. A vector embedding is just a long list of numbers, each describing a feature of the data object.</p> <p>An example is how we numerically represent colors <a href="https://huggingface.co/spaces/jphwang/colorful_vectors">in the RGB system</a>, where each number in the vector describes how red, green, or blue a color is. E.g., the following green color can be represented as [6, 205, 0] in the RGB system.</p> <p><img src="img/Untitled.png" alt="RGB system"/></p> <p>But fitting more complex data, such as words, sentences, or text, into a meaningful series of numbers isn’t trivial. This is where AI models come in: AI models enable us to represent the contextual meaning of, e.g., a word as a vector because they have learned to represent the relationship between different words in a vector space. AI models that can generate embeddings from unstructured data are also called embedding models or vectorizers. Below, you can see an example of the three-dimensional vector space of the RGB system with a few sample data points (colors).</p> <p><img src="img/Untitled-1.png" alt="rgb-color-vector-space"/></p> <p>Different embedding models represent data in different vector spaces. Vector spaces can differ in the number of dimensions, ranging from tens to thousands, where the precision of the representation increases with increasing dimensions. In the color example, there are also different ways to represent a color numerically. For example, you can represent the previous green color in the RGB system as [6, 205, 0] or in the CMYK system as [97, 0, 100, 20].</p> <p>This is why it is important to use the same embedding model for all your data to ensure it is in the respective vector space.</p> <p>Vector embeddings numerically capture the semantic meaning of the objects in relation to other objects. Thus, similar objects are grouped together in the vector space, which means the closer two objects, the more similar they are.</p> <p>For now, let’s consider a simpler example with numerical representations of words - also called word vectors. In the following image, you can see the words “Wolf” and “Dog” close to each other because dogs are direct descendants of wolves. Close to the dog, you can see the word “Cat,” which is similar to the word “Dog” because both are animals that are also common pets. But further away, on the right-hand side, you can see words that represent fruit, such as “Apple” or “Banana”, which are close to each other but further away from the animal terms.</p> <p><img src="img/vector-embeddings.jpg" alt="vector embeddings"/></p> <h3 id="vector-search">Vector Search</h3> <p>Vector embeddings allow us to find and retrieve similar objects by searching for objects that are close to each other in the vector space. This concept is called <a href="/blog/vector-search-explained">vector search</a>, similarity search, or semantic search.</p> <p>Similarly to how we can find similar vectors for the word “Dog”, we can find similar vectors to a search query. For example, to find words similar to the word “Kitten”, we can generate a vector embedding for this query term - also called a query vector - and retrieve all its nearest neighbors, such as the word “Cat”, as illustrated below.</p> <p><img src="img/vector-search.jpg" alt="Vector search"/></p> <p>As the concept of semantic search is based on the contextual meaning, it allows for a more human-like search experience by retrieving relevant search results that match the user’s intent. This advantage makes vector search important for applications, that are e.g., sensitive to typos or synonyms.</p> <p>The numerical representation of a data object allows us to apply mathematical operations to them. For example you can calculate the distance between two vector representations to determine their similarity. You can use several similarity measures to calculate the distance between two vectors. E.g., Weaviate supports the following distance metrics:</p> <ul> <li><strong>Squared Euclidean or L2-squared distance</strong> calculates the straight-line distance between two vectors. Its range is [0, ∞], where 0 represents identical vectors, and larger values represent increasingly dissimilar vectors.</li> <li><strong>Manhattan or L1 distance</strong> calculates the sum of the lengths of the projections of the line segment between the points onto the coordinate axes. Its range is [0, ∞], where 0 represents identical vectors, and larger values represent increasingly dissimilar vectors.</li> <li><strong>Cosine similarity</strong> calculates the cosine of the angle between two vectors. Weaviate uses the cosine distance for the complement of cosine similarity. Its range is [0, 2], where 0 represents identical vectors, and 2 represents vectors that point in opposite directions.</li> <li><strong>Dot product</strong> calculates the product of the magnitudes of two vectors and the cosine of the angle between them. Its range is [-∞, ∞], where 0 represents orthogonal vectors, and larger values represent increasingly similar vectors. Weaviate uses the negative dot product to keep the intuition that larger values represent increasingly dissimilar vectors.</li> <li><strong>Hamming distance</strong> calculates the number of differences between vectors at each dimension.</li> </ul> <p><img src="img/similarity-measures.jpg" alt="distance metrics"/></p> <p>As you can see, there are many different similarity measures. As a rule of thumb, select the same metric as the one used to train the ML model. Suppose you are unsure which one was used. In that case, you can usually find this information on the hosting service’s site, e.g., <a href="https://platform.openai.com/docs/guides/embeddings/which-distance-function-should-i-use">OpenAI’s ada-002 uses cosine similarity</a> or <a href="https://huggingface.co/sentence-transformers/multi-qa-MiniLM-L6-cos-v1#technical-details">MiniLM hosted on Hugging Face supports cosine similarity, dot product, and Euclidean distance</a>. To learn more about the different distance metrics, you can continue reading our blog post on <a href="/blog/distance-metrics-in-vector-search">What are Distance Metrics in Vector Search?</a></p> <h3 id="vector-indexing-for-approximate-nearest-neighbor-approach">Vector Indexing for Approximate Nearest Neighbor Approach​</h3> <p>Vector indexing is the process of organizing vector embeddings so that data can be retrieved efficiently.</p> <p>When you want to find the closest items to a given query vector, the brute force approach would be to use the k-Nearest Neighbors (kNN) algorithm. But calculating the similarity between your query vector and every entry in the vector database requires a lot of computational resources, especially if you have large datasets with millions or even billions of data points. This is because the required calculations increase linearly (O(n)) with the dimensionality and the number of data points.</p> <p>A more efficient solution to find similar objects is to use an <strong>approximate nearest neighbor (ANN) approach</strong>. The underlying idea is to pre-calculate the distances between the vector embeddings. Then you can organize and store similar vectors close to each other (e.g., in clusters or a graph), so that you can later find similar objects faster. This process is called vector indexing. Note that the speed gains are traded in for some accuracy because the ANN approach returns only the approximate results.</p> <p>In the previous example, one option would be to pre-calculate some clusters, e.g., animals, fruits, and so on. So, when you query the database for “Kitten”, you could start your search by only looking at the closest animals and not waste time calculating the distances between all fruits and other non-animal objects. More specifically, the ANN algorithm can help you to start the search in a region near, e.g., four-legged animals. Then, the algorithm would also prevent you from venturing further from relevant results.</p> <p><img src="img/vector-indexing.jpg" alt="Vector indexing for Approximate Nearest Neighbor"/></p> <p>The above example roughly describes the Hierarchical Navigable Small World (HNSW) algorithm. It is also the default ANN algorithm in Weaviate. However, there are several ANN algorithms to index the vectors, which can be categorized into the following groups:</p> <ul> <li>Clustering-based index (e.g., <a href="https://github.com/facebookresearch/faiss">FAISS</a>)</li> <li>Proximity graph-based index (e.g., <a href="https://arxiv.org/abs/1603.09320">HNSW</a>)</li> <li>Tree-based index (e.g., <a href="https://github.com/spotify/annoy">ANNOY</a>)</li> <li>Hash-based index (e.g., <a href="https://en.wikipedia.org/wiki/Locality-sensitive_hashing">LSH</a>)</li> <li>Compression-based index (e.g., <a href="https://ieeexplore.ieee.org/document/5432202">PQ</a> or <a href="https://ai.googleblog.com/2020/07/announcing-scann-efficient-vector.html">SCANN</a>)</li> </ul> <p>Note that indexing enables fast retrieval at query time, but it can take a lot of time to build the index initially.</p> <p>Further reading: <a href="/blog/vector-search-explained">Why Is Vector Search So Fast</a></p> <h2 id="vector-database-architecture">Vector Database Architecture</h2> <p>Since vector databases do not only store vector embeddings but also the original data objects, their architecture consists of multiple indices.</p> <p>Below you can see an example of the <a href="https://docs.weaviate.io/weaviate/concepts#weaviate-architecture">Weaviate vector database architecture</a>. <img src="img/database_architecture.png" alt="Database architecture"/></p> <p>Users can create any number of indices and each index can contain any number of shards. Shards can be dsitributed and/or replicated across nodes in the cluster. A shard always contains object, inverted, and vector storage. Vector storage is not affected by LSM segmentation.</p> <h2 id="vector-database-use-cases">Vector Database Use Cases</h2> <p>A wide variety of applications use the search capabilities of vector databases. They range from classical ML use cases, such as natural language processing (NLP), computer vision, and recommender systems, to providing long-term memory to LLMs in modern applications.</p> <p>The <strong>most popular use case of vector databases is naturally for search</strong>. Because a vector search can help find similar objects, it is predestined for applications where you might want to find similar products, movies, books, songs, etc. That’s why they are also <strong>used in recommendation systems</strong> as a restated search task.</p> <p>With the rise of LLMs, <strong>many modern Generative AI applications use vector databases as an external knowledge source</strong>. You might have already come across the term that they provide LLMs with long-term memory. LLMs are stateless, which means that they immediately forget what you have just discussed if you don’t store this information in, e.g., a vector database and thus provide them with a state. This enables LLMs to hold an actual conversation. Also, you can store additional information in them as part of a<a href="/blog/introduction-to-rag">retrieval-augmented generation (RAG)</a> pipeline to reduce hallucinations.</p> <p>Here is an example <a href="https://github.com/weaviate/healthsearch-demo">demo</a> called <a href="https://weaviate.io/blog/healthsearch-demo">HealthSearch</a> of a <a href="http://weaviate.io/">Weaviate</a> vector database in action together with an LLM showcasing the potential of leveraging user-written reviews and queries to retrieve supplement products based on specific health effects.</p> <p><img src="img/image8.png" alt="example-use-case-llm-vector-database"/></p> <h2 id="tool-landscape-around-vector-databases">Tool Landscape around Vector Databases​</h2> <p>Although vector databases are AI-native and specifically designed to handle vector embeddings and enable efficient vector search, alternatives like <a href="https://weaviate.io/blog/vector-library-vs-vector-database">vector libraries</a> and vector-capable databases exist as well.</p> <h3 id="vector-database-vs-traditional-relational-database">Vector Database vs. Traditional (Relational) Database​</h3> <p>The main difference between a modern vector and a traditional (relational) database comes from the type of data they were optimized for. While a relational database is designed to store structured data in columns, a vector database is also optimized to store unstructured data (e.g., text, images, or audio) and their vector embeddings.</p> <p>Because vector and relational databases are optimized for different types of data, they also differ in how data is stored and retrieved. In a relational database, data is stored in columns and retrieved by keyword matches in traditional search. In contrast, vector databases also store the original data’s vector embeddings, enabling efficient semantic search. Because vector search can semantically understand your search terms, it doesn’t rely on retrieving relevant search results based on exact matches. This makes it robust to synonyms.</p> <p>For example, imagine you have a database that stores Jeopardy questions, and you want to retrieve all questions that contain an animal. Because search in traditional databases relies on keyword matches, you would have to create a big query that queries all animals (e.g., contains “dog”, or contains “cat”, or contains “wolf”, etc.). With semantic search, you could simply query for the concept of “animals”.</p> <p><img src="img/Traditional_Search_X_Vector_Search.jpg" alt="Traditional search vs. vector search"/></p> <p>Because most vector databases do not only store vector embeddings but store them together with the original source data, they are not only capable of vector search but also enable traditional keyword search. Some, like Weaviate, even support <a href="https://weaviate.io/blog/hybrid-search-explained">hybrid search</a> capabilities that combine vector search with keyword search.</p> <h3 id="vector-database-vs-vector-capable-database-sql-and-nosql">Vector Database vs. Vector-Capable Database (SQL and NoSQL)​</h3> <p>Today, many existing databases have already enabled vector support and vector search. However, they usually don’t index the vector embeddings, which makes the vector search slow. Thus, an advantage of AI-native vector databases over vector-capable databases is their efficiency in vector search due to vector indexing.</p> <h3 id="vector-database-vs-vector-indexing-library">Vector Database vs. Vector Indexing Library​</h3> <p>Similarly to vector databases, <a href="https://weaviate.io/blog/vector-library-vs-vector-database">vector libraries</a> also enable fast vector search. However, vector libraries only store vector embeddings of data objects, and they store them in in-memory indexes. This results in two key differences:</p> <ol> <li>Updatability: The index data is immutable, and thus, no real-time updates are possible.</li> <li>Scalability: Most vector libraries cannot be queried while importing your data, which can be a scalability concern for applications that require importing millions or even billions of objects.</li> </ol> <p>Thus, vector libraries are a great solution for applications with a limited static snapshot of data. However, if your application requires real-time scalable semantic search at the production level, you should consider using a vector database.</p> <h3 id="vector-database-vs-graph-database">Vector Database vs. Graph Database​</h3> <p>A graph database stores data in nodes and edges, representing entities and their relationships. It’s optimized for querying connections and patterns within the data, making it powerful for network, social, and recommendation systems. A vector database, on the other hand, indexes, stores, and provides access to structured or unstructured data (e.g., text or images) alongside its vector embeddings, which are the data’s numerical representation. It allows users to find and retrieve similar objects quickly at scale in production.</p> <h3 id="vector-database-vs-vector-store">Vector Database vs. Vector Store</h3> <p>A vector store is a lighter system or library designed specifically to store and retrieve vector embeddings. It focuses more on managing vectors and supports basic operations like indexing and querying. A vector database, on the other hand, not only stores and retrieves vector embeddings but also offers comprehensive database functionalities such as distributed storage, scaling, security, and support for complex queries.</p> <h2 id="vector-database-faqs">Vector Database FAQs</h2> <p>This section answers some common questions about vector databases.</p> <h3 id="why-we-need-vector-databases">Why we need vector databases</h3> <p>Vector databases are essential for efficiently managing and searching high-dimensional vector embeddings. They enable real-time accurate similarity searches, which are critical in the AI-native app stack.</p> <h3 id="why-use-a-vector-database">Why Use a Vector Database?​</h3> <p>Vector databases have become a critical component in the AI-native app stack, particularly for managing the vast amounts of unstructured data that organizations generate, such as images, text, and audio. Unlike traditional databases, which struggle with unstructured data, vector databases efficiently store, search, and analyze this data by leveraging high-dimensional vector representations or embeddings. This capability is particularly beneficial where the ability to retrieve relevant information quickly and accurately is critical.</p> <p>For example, in recommendation systems or NLP tasks, vector databases enable businesses to unlock valuable insights, leading to more informed decision-making and improved personalization. Furthermore, they enable human-like semantic search capabilities, allowing businesses to find products, content, or information that closely match a user’s query. This not only improves user experience and engagement but also drives customer-specific personalization.</p> <p>Vector databases’ scalability and performance are crucial as businesses grow and data volumes increase. These databases are optimized for handling large-scale, high-dimensional vectors, ensuring that data retrieval remains fast and accurate even at scale. This scalability is vital for AI applications that require real-time data processing, such as chatbots, fraud detection, and other interactive AI systems.</p> <p>Additionally, vector databases integrate seamlessly with existing AI workflows, including LLM orchestration frameworks, embedding model providers, and inference engines. This integration simplifies the deployment and scaling of production-level RAG applications, making it easier for businesses to harness AI’s full potential. By providing a specialized infrastructure for storing and retrieving vectors, vector databases reduce the complexity and cost associated with AI development, allowing organizations to focus on innovation and delivering personalized experiences to their customers.</p> <h3 id="how-to-use-a-vector-database">How to use a vector database</h3> <p>Store vector embeddings in the database, index them for efficient retrieval and perform similarity searches to find the most relevant data points.</p> <h3 id="how-to-choose-a-vector-database">How to choose a vector database</h3> <p>Selection criteria for vector databases are search latency, memory footprint as object count scales up, recall and retrieval relevancy, cost efficiency, and integration capabilities with your existing stack and AI tools, such as language models and embedding models.</p> <h2 id="summary">Summary​</h2> <p>This article explained that vector databases are a type of database that indexes, stores, and provides access to structured or unstructured data alongside its vector embeddings. Vector databases like <a href="http://weaviate.io/">Weaviate</a> allow for efficient similarity search and retrieval of data based on their vector distance or vector similarity at scale.</p> <p>We covered their core concepts, such as vector embeddings, and discussed that they enable efficient vector search by leveraging ANN algorithms. Additionally, we explored other vector search tools and discussed the advantages of vector databases over traditional and vector-capable databases and vector libraries.</p> <p><strong>Last Updated On</strong>: November 189th, 2024</p> <p>import WhatNext from ‘/_includes/what-next.mdx’</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="concepts"/><summary type="html"><![CDATA[What is a Vector Database? Explaination of core concepts, such as vector embeddings, vector search, and vector indexing]]></summary></entry><entry><title type="html">Multimodal Embedding Models</title><link href="https://zainhas.github.io/blog/2023/multimodal-models/" rel="alternate" type="text/html" title="Multimodal Embedding Models"/><published>2023-06-27T00:00:00+00:00</published><updated>2023-06-27T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2023/multimodal-models</id><content type="html" xml:base="https://zainhas.github.io/blog/2023/multimodal-models/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2023-06-27-multimodal-models/hero-480.webp 480w,/assets/img/posts/2023-06-27-multimodal-models/hero-800.webp 800w,/assets/img/posts/2023-06-27-multimodal-models/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2023-06-27-multimodal-models/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Multimodal Embedding Models" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="the-multisensory-nature-of-human-learning">The Multisensory Nature of Human Learning</h2> <p>Humans have a remarkable ability to learn and build world models through the integration of multiple sensory inputs. Our combination of senses work synergistically to provide us with rich and diverse information about our environment. By combining and interpreting these sensory inputs, we are able to form a coherent understanding of the world, make predictions, and acquire new knowledge very efficiently.</p> <p>The process of learning through multi-sensory inputs begins from the early stages of human development. Infants explore the world through their senses, touching, tasting, listening, and observing objects and people around them. This sensory exploration helps them link different perspectives of the same experience to create a holistic understanding of their environment.</p> <p>This fusion of multisensory data when learning new concepts is also partially responsible for why humans can learn with very few data points - making us great few-shot learners. Let’s imagine you are trying to teach the concept of a dog to a child. The next time you see a dog at the park you point it out and say “This is a dog!”. Let’s say that this is a single observation/data point - in the supervised machine-learning sense. From this single data point, the child receives an abundance of information: they see how the dog moves, interacts, and reacts to the world around it, hear how the dog barks, see how the fur on the dog blows in the wind, can touch the dog to see how it feels and even smell the dog. So, from this single “data point” the child extracts a very rich representation of multiple interdependent modalities of data that very distinctly define the concept of a dog.</p> <p><img src="/assets/img/posts/2023-06-27-multimodal-models/dogs.jpg" alt="dogs"/></p> <p>Over time and with age, this fusion of sensory inputs gets more refined and nuanced and allows infants to develop higher levels of abstraction and understanding of objects’ properties, such as shape, texture, and weight.</p> <p>Humans are such good multimodal reasoning engines that we do it without noticing - let’s consider a practical scenario. Imagine you’re on an airplane and only have your wireless headset that cannot be plugged into the in-flight entertainment system - a predicament I find myself in more often than not these days!😅 So you start watching a movie with no sound and for the most part, you notice that you can get a pretty good, although imperfect, understanding of what’s going on. Now imagine you turn on the captions, and now you can understand pretty much everything that’s going on and you can even fill in sound effects with your imagination supported by the modalities of data you have available.</p> <p>In order for our machine learning models to interact more naturally with data, the way we do, and ultimately be more general and powerful reasoning engines we need them to understand a data point through its corresponding image, video, text, audio, tactile and other representations - it needs to be able to preserve the meaning behind all of these data modalities once embedded in high dimensional vector space as demonstrated in Figure1 below. For example, in order to understand what a train is, we need our ML models to “see” and “hear” the train move, “feel” the motion of the ground near the train, and “read” about it. This is easier said and understood than done. Having a model that jointly comprehends all of these data modalities is very difficult, let’s discuss some of the challenges that stand between us and truly multimodal models.</p> <figure> ![vectorspace](/assets/img/posts/2023-06-27-multimodal-models/vectorspace.png) <figcaption>Figure 1. Shows a joint embedding space of a multimodal model that understands both text and images. Notice how objects that are similar are closer together and dissimilar objects are farther apart, this means that the model preserves semantic similarity within and across modalities.</figcaption> </figure> <h2 id="addressing-challenges-of-learning-multimodal-embeddings">Addressing Challenges of Learning Multimodal Embeddings</h2> <h3 id="1-lack-of-rich--aligned-multimodal-datasets">1. Lack of Rich &amp; Aligned Multimodal Datasets</h3> <p>Collecting and preparing multimodal datasets is very challenging. Each modality requires specific data collection techniques and preprocessing steps. Furthermore aligning and normalizing the data across modalities is crucial to ensure compatibility. This is quite challenging due to differences in data formats, temporal alignment, and semantic alignment. If the data is stitched together from different datasets and is not aligned properly then it becomes very difficult for the machine-learning model to extract and learn interdependencies between the modalities.</p> <p>Current approaches address this by taking multiple rich datasets and fusing them across data modalities where possible. So for example you might be able to combine the image/video of a lion from a computer vision dataset with the roar of a lion from an audio dataset but perhaps not with motion since you might not have motion data for a lion. There are also attempts to collect richer multimodal datasets such as the <a href="https://ego4d-data.org/docs/">EGO4D</a> which collects multiple data modalities for a given scenario. EGO4D actually captures motion/tactile data by capturing accelerometer and gyroscopic data temporally aligned with video captures of activities! The problem with this is that generating a rich multimodal dataset is very costly and even impractical in some cases.</p> <h3 id="2-model-architecture">2. Model Architecture</h3> <p>Designing the architecture for a single model that can process multimodal data is difficult. Usually, machine learning models are specialists in one domain of data - for example computer vision or natural language. Training a single, jack-of-all-modalities model is very difficult.</p> <p>Current multimodal approaches like <a href="https://arxiv.org/abs/2305.05665">ImageBind</a> from FAIR, Meta AI(which combines image, speech, text, video, motion and depth/thermal data) approach this problem by taking separate specialist pre-trained models for each modality and then finetuning them to bind their latent space representations using a contrastive loss function; which essentially pulls together representations of similar examples across modalities closer together and pushes apart distinct examples in a joint vector space. The key insight is that all combinations of paired modalities are not necessary to train such a joint embedding, and only image-paired data is sufficient to align and bind the modalities together. The limitation however is that adding and finetuning a separate pretrained model for every modality or task becomes prohibitively expensive and is not scalable.</p> <h3 id="3-model-interpretability">3. Model Interpretability</h3> <p>Understanding and interpreting the decisions made by multimodal models can be challenging. The fusion of different modalities may introduce complexities in interpreting the learned representations and attributing importance to each modality. Consider, for example, searching in the learned joint embedding space of a multimodal model what accelerometer motion data is closest to a person giving a speech about globalization. The lesson is that some modalities or data objects just don’t pair together naturally.</p> <p>The ImageBind model handles this by using the image representation as ground truth and pulling all other concepts closer to image representations which then establishes a naturally learned alignment between other modalities. These image representations are initialized with large-scale vision-language models such as CLIP, thereby leveraging the rich image and text representations of these models. Some of these alignments are explained directly (comparing a video of a person delivering a speech to audio of the speech) while other pairs of modalities are explained through their relation to an intermediate modality(understanding the relation between motion data and text data by seeing how they both relate to video/image data). With that said in other cases a logical explanation of why two examples are close together might not be possible without a close examination of the training sets.</p> <h3 id="4-heterogeneity-and-training-optimization">4. Heterogeneity and Training Optimization</h3> <p>Handling modality imbalance(we have a lot of image and text data and significantly less motion and tactile data) makes it very difficult to learn all modalities equally well; this takes careful tuning of the contribution of each modality during training and optimizing the fusion mechanism to prevent dominance by a single modality. Data imbalance is one consideration but even more important to consider is how much unique information a modality brings - this is known as heterogeneity. Assessing heterogeneity accurately allows you to decide which modalities are different enough to be separately processed and which modality pairs interact differently and thus should be differently fused.</p> <p><a href="https://arxiv.org/abs/2203.01311">HighMMT</a> has been proposed to handle high-modality scenarios involving a large set of diverse modalities. HighMMT uses two new information-theoretic metrics for heterogeneity quantification, enabling it to automatically prioritize the fusion of modalities that contain unique information or unique interactions. This results in a <strong>single model</strong> that scales up to 10 modalities and 15 tasks from 5 different research areas, demonstrating a crucial scaling behavior not found in ImageBind: performance continues to improve with each modality added, and it transfers to entirely new modalities and tasks during fine-tuning.</p> <p>In conclusion, efforts on developing multimodal models attempt to mimic human learning by combining different inputs, such as images, text, and audio, to improve the performance and robustness of machine learning systems. By leveraging multi-sensory inputs, these models can learn to recognize complex multimodal patterns, understand context across modes, and generate more comprehensive and accurate outputs even in the absence of some modalities. The main goal is to give these models the ability to interact with data in a more natural way thus enabling them to be more powerful and general reasoning engines.</p> <h2 id="multimodal-models-in-weaviate">Multimodal Models in Weaviate</h2> <p>Currently, the only out-of-the-box multimodal module that can be configured and used with Weaviate is <code class="language-plaintext highlighter-rouge">multi2vec-clip</code> which can be used to project images and text into a joint embedding space and then perform a <code class="language-plaintext highlighter-rouge">nearVector</code> or <code class="language-plaintext highlighter-rouge">nearImage</code> search over these two modalities. Outside of this, you can only use multimodal models if they are hosted on Huggingface or if you have your own proprietary multimodal models. We know that our users love multimodal models and we’ve been cooking up more OOTB integrations with cutting-edge multimodal models so that it’s easy for you to start using them in your vector search pipelines. Be on the lookout for new multimodal search capabilities soon, I “hear” it’s going to be amazing.😉</p> <p>import WhatNext from ‘/_includes/what-next.mdx’</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="concepts"/><summary type="html"><![CDATA[ML Models that can see, read, hear and more!]]></summary></entry><entry><title type="html">Running Large Language Models Privately - privateGPT and Beyond</title><link href="https://zainhas.github.io/blog/2023/private-LLM/" rel="alternate" type="text/html" title="Running Large Language Models Privately - privateGPT and Beyond"/><published>2023-05-30T00:00:00+00:00</published><updated>2023-05-30T00:00:00+00:00</updated><id>https://zainhas.github.io/blog/2023/private-LLM</id><content type="html" xml:base="https://zainhas.github.io/blog/2023/private-LLM/"><![CDATA[<div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/2023-05-30-private-LLM/hero-480.webp 480w,/assets/img/posts/2023-05-30-private-LLM/hero-800.webp 800w,/assets/img/posts/2023-05-30-private-LLM/hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/2023-05-30-private-LLM/hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Running Large Language Models Privately - privateGPT and Beyond" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Large Language Models (LLM’s) have revolutionized how we access and consume information, shifting the pendulum from a search engine market that was predominantly retrieval-based (where we asked for source documents containing concepts relevant to our search query), to one now that is growingly memory-based and performs generative search (where we ask LLMs to generate answers to questions based on their knowledge of and training on massive datasets). More importantly, what’s different this time is that with the advent of widely accessible products like ChatGPT that have exposed the underlying technology of these models to the wider consumer market, we’re seeing these models revolutionize how we work, learn and interact on a scale never seen before.</p> <p>This wide-scale adoption of LLMs makes the concerns and challenges around privacy and data security paramount, and ones that each organization needs to address. In this blog post we will explore some of the different potential approaches organizations can take to ensure robust data privacy while harnessing the power of these LLMs.</p> <h2 id="understanding-the-privacy-challenge">Understanding the Privacy Challenge</h2> <p>LLMs are typically trained on vast amounts of data to develop a statistical understanding of human language patterns. If you’d like to get an introductory explanation of how these models work, please read our previous blogpost: <a href="/blog/what-are-llms">How LLMs Work</a>. The extensive datasets used in training can often contain sensitive information, leading to privacy concerns. Additionally, the traditional approach of relying on cloud-based services to deploy and conduct inference with these models requires organizations to transfer their data to third-party centralized model providers which can lead to data exposure, data breaches, and unauthorized access. This is the very reason large corporations such as Samsung, Apple, Verizon, JPMorgan and many more have <a href="https://www.hr-brew.com/stories/2023/05/11/these-companies-have-banned-chatgpt-in-the-office">limited their employees from using these services</a>.</p> <p>To leverage the advantages of generative AI while simultaneously addressing these privacy concerns, the field of privacy-preserving machine learning has emerged, offering techniques and tools that allow for the secure execution of large language models while protecting the confidentiality of sensitive data both during model fine-tuning as well as when providing responses grounded in proprietary data.</p> <h2 id="potential-solutions-to-the-privacy-challenge">Potential Solutions to the Privacy Challenge</h2> <h3 id="federated-learning">Federated Learning</h3> <p><img src="/assets/img/posts/2023-05-30-private-LLM/federated.png" alt="Federated"/></p> <p>Federated Learning enables model training without directly accessing or transferring user data. Instead, individual edge devices or servers collaboratively train the model while keeping the data local. This approach ensures that sensitive data remains private, reducing the risk of data breaches during model fine-tuning on custom data.</p> <p>More specifically, federated learning is a distributed approach to model training that allows multiple parties to collaborate without the need for centralized data sharing. In traditional machine learning, data is typically collected, centralized, and used for training a model. However, in scenarios where data privacy is a concern, federated learning offers a privacy-preserving alternative.</p> <p>The core idea behind federated learning is to bring the model training process to the data instead of moving the data to a central location where model training occurs. This decentralized approach ensures that sensitive data remains local and does not need to be exposed or transferred. Instead, individual devices or servers participate in the training process by sending model updates to a central server, which aggregates and incorporates these updates to improve the global model.</p> <p>The main challenge here is that training LLMs in central locations with access to large amounts of optimized computing is hard enough, and doing this in a distributed manner significantly complicates matters. You now have to worry about the heterogeneity of the data available on different edge devices that the model is training on, how to securely aggregate the learned weights from all the different edge devices, how to prevent adversaries from inferring private data from the individual model updates, as well as model compression and efficiency since now these edge devices like mobile phones have to upload model updates to be aggregated globally.</p> <h3 id="homomorphic-encryption">Homomorphic Encryption</h3> <p><img src="/assets/img/posts/2023-05-30-private-LLM/encryption.png" alt="Encryption"/></p> <p>Homomorphic encryption (HE) allows computations to be performed on encrypted data without decrypting it. It is a powerful tool for preserving privacy in scenarios where sensitive data needs to be processed or analyzed while maintaining confidentiality. This technique can be applied to LLMs, enabling private inference while preserving the confidentiality of user inputs. However, it’s worth noting that homomorphic encryption can introduce computational overhead, impacting the model’s performance.</p> <p>In traditional encryption, encrypted data can only be operated on in its decrypted form. HE, on the other hand, enables computations to be carried out directly on encrypted data, producing encrypted results that can be decrypted to obtain the same outcome as if the computations were performed on the original, unencrypted data.</p> <p>HE enables the secure outsourcing of data processing tasks to third-party service providers or cloud platforms. Data owners can delegate computations without revealing the underlying data, ensuring confidentiality while benefiting from the superior computational resources available to large cloud providers. Allowing computations on encrypted data enables training and inference on sensitive datasets without revealing their contents. HE seems to be a very promising way of achieving privacy-preserving LLM usage simply by encrypting the prompt token and decrypting the generated response.</p> <p>On a practical level however HE does have some disadvantages: it is much harder to implement LLM training and inference with HE data than on unencrypted data, and more compute is required to process encrypted data, which increases processing times and further increases compute requirements which are already very high. You also lose out on model quality and accuracy when training/inferencing on encrypted data (this is because the encryption process adds noise to the data) and thus you have to balance privacy vs. model performance/utility.</p> <h3 id="locally-deployed-llms">Locally Deployed LLMs</h3> <p>Another option is to run open-source LLMs locally vs running models that can only be accessed by general-purpose black-box APIs. Out of all of the privacy-preserving machine learning techniques presented thus far, this is perhaps the most production-ready and practical solution organizations can implement today. There are already some preliminary solutions that are publicly available that allow you to deploy LLMs locally, including <a href="https://github.com/imartinez/privateGPT">privateGPT</a> and <a href="https://github.com/h2oai/h2ogpt">h2oGPT</a>. These solutions, which are currently proof of concepts, are taking advantage of the growing zoo of open-source LLMs, including the famous <a href="https://arxiv.org/abs/2302.13971">LLaMA</a> model from Meta, which is proven to perform quite well with some limited fine-tuning, as presented in this <a href="https://arxiv.org/abs/2305.11206">recent paper</a>.</p> <p>Let’s dig deeper into locally deployed LLMs, and see how companies can more securely leverage the power of open source software (OSS) LLMs on their own proprietary documents, all in the privacy of their on-prem or hybrid servers.</p> <h2 id="locally-running-llms-with-custom-data">Locally Running LLMs With Custom Data</h2> <p>Prior to discussing how you can run OSS LLMs locally, let’s discuss how you can get any LLM, local or remote, to answer prompts grounded in your custom data. The key ingredient here is to store the documents that you want to use to provide the LLM custom context, in a <a href="/blog/what-is-a-vector-database">vector database</a> so that when needed the LLM can look up and retrieve the relevant documents and consume them to learn more context prior to generating a prompt. This not only provides the LLM custom data and context where it previously had none, but it also prevents hallucinations, since you help provide the LLM with relevant information, greatly reducing the chances it will make something up to fulfill the prompt. This process is known as <a href="https://arxiv.org/pdf/2005.11401.pdf">retrieval augmented generation (RAG)</a> since you are augmenting the generation process of the LLM with retrieved documents from your vector database.</p> <p>Currently, RAG is a 4-step process illustrated in the figure below:</p> <figure> ![fig1](/assets/img/posts/2023-05-30-private-LLM/fig1.png) <figcaption> Figure 1. Step 1&amp;2: Query your remotely deployed vector database that stores your proprietary data to retrieve the documents relevant to your current prompt. Step3&amp;4: Stuff the returned documents along with the prompt into the context tokens provided to the remote LLM; which it will then use to generate a custom response. </figcaption> </figure> <p>As shown in the above image, this process requires you to remotely store your documents in a cloud-hosted vector database and also call an API which allows you to prompt a remotely deployed LLM. This exact workflow can be replicated using <a href="https://console.weaviate.cloud/">Weaviate Cloud</a> and any one of the generative modules (<a href="https://docs.weaviate.io/weaviate/model-providers/openai/generative">OpenAI</a>, <a href="https://docs.weaviate.io/weaviate/model-providers/cohere/generative">Cohere</a>, <a href="https://docs.weaviate.io/weaviate/model-providers/google/generative">PaLM</a>).</p> <p>Now let’s discuss how you can modify this setup to achieve a completely local vector database and LLM setup. The first step to a fully local setup is to bring our proprietary data into our local environment. For some organizations, this means running the vector database on premises within their walls. For others this can mean running the vector database on their own self-managed virtual private cloud (VPC). Either of these workflows can be set up with a vector database like Weaviate, enabling a secure and private solution where data included in Step 1&amp;2 doesn’t leave your organization. The generative modules mentioned above can also be used in this setup seamlessly but note that in this setup, retrieved documents included with the prompt will need to be shared with the LLM that is remotely hosted. This is what our modified workflow now looks like:</p> <figure> ![fig2](/assets/img/posts/2023-05-30-private-LLM/fig2.png) <figcaption> Figure 2. Step 1&amp;2: Query your locally deployed vector database that stores your proprietary data to retrieve the documents relevant to your current prompt. Step3&amp;4: Stuff the returned documents along with the prompt into the context tokens provided to the remote LLM; which it will then use to generate a custom response. </figcaption> </figure> <p>In order to bring the LLMs into your local environment you need to have access to their weights so that you can perform inference with them locally on demand. As a result, you can only use open-source models along with vector databases that can be deployed on-prem or within your VPC for this next setup displayed in Figure 3 below.</p> <figure> ![fig3](/assets/img/posts/2023-05-30-private-LLM/fig3.png) <figcaption> Figure 3. Step 1&amp;2: Query your locally deployed vector database that stores your proprietary data to retrieve the documents relevant to your current prompt. Step3&amp;4: Stuff the returned documents along with the prompt into the context tokens provided to the locally running OSS LLM; which it will then use to generate a custom response. </figcaption> </figure> <p>With this above setup, now we can conduct RAG completely locally and privately. In order for you to play around with this setup, we have developed a Weaviate integration for privateGPT that implements that above setup <a href="https://github.com/hsm207/privateGPT">here</a>. This integration allows you to vectorize, ingest and query your own custom documents with open source models, as well as having Weaviate act as a vector store, completely locally. You can run this demo offline if you wanted once you have all the required dependencies. If you implement the above demo, one of the limitations you’ll find is the remarkably slow inference times when running the LLM on your own machine. Let’s discuss some of the advantages and disadvantages of the above fully local private setup that can be used to perform RAG on your proprietary data.</p> <h2 id="advantages-and-disadvantages-of-a-localprivate-setup">Advantages and Disadvantages of a Local/Private Setup</h2> <p>The advantages of locally deploying your vector database and LLM models first and foremost is the data privacy guarantee: user and proprietary data now remain within the local infrastructure, reducing chances of exposure to external entities and mitigating third-party risk. If you keep your VectorDB+LLM local, another advantage is the reduction of the attack surface and potential vulnerabilities associated with network communications and unauthorized data access. These privacy guarantees can facilitate compliance with data protection and privacy regulations which are pivotal for regulated businesses that operate in healthcare, finance, etc.</p> <p>On the other hand, a RAG stack running locally or on your VPC is constrained by the compute and resources that you can make available to it. This is the main reason the above privateGPT demo with Weaviate might run quite slowly on your own machines. Organizations need to invest in high-performance hardware, such as powerful servers or specialized hardware accelerators, to handle the computational demands. This can result in high upfront costs, ongoing maintenance expenses as well as difficulty scaling quickly if needed. Another disadvantage of bringing the tech stack local is the responsibility of infrastructure maintenance, system administration, security updates as well as machine learning-specific model updates which require extensive technical expertise. For this reason, this solution is great for larger companies like Samsung, Apple, and JPMorgan that have the budget to afford the required compute and expertise, and less so for resource-constrained smaller-scale companies.</p> <h2 id="conclusion">Conclusion</h2> <p>The advent of large language models and consumer-wide adoption has brought tremendous progress, but a by-product of this large-scale adoption is that privacy concerns have also become more pronounced. However, advances in privacy-preserving techniques have made it possible to harness the power of large language models while upholding the highest standards of data privacy and security.</p> <p>In this blog post, we discussed some cutting-edge approaches to privacy-preserving ML, such as federated learning and homomorphic encryption (which are currently being developed and have great potential in the future), as well as a more practical approach that can take advantage of OSS LLMs and vector databases to power generative search and custom chatbots locally and privately.</p> <p>As we move forward, it is crucial for the AI community to continue prioritizing privacy and security, ensuring that LLMs can be deployed in a manner that respects individual privacy rights.</p> <p>import WhatNext from ‘/_includes/what-next.mdx’</p> <WhatNext/>]]></content><author><name></name></author><category term="technical-blog"/><category term="concepts"/><category term="how-to"/><summary type="html"><![CDATA[A discussion on data privacy and privacy-preserving machine learning for LLMs]]></summary></entry></feed>